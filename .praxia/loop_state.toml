# .praxia/loop_state.toml — SEED / READ-ONLY FALLBACK / TOMBSTONE
# This file is the initialization seed: `praxia loop migrate` promotes it to the DB row's state_data.
# After migration: DO NOT hand-edit this file for loop operation. It is a read-only fallback (DB-down) and
# tombstone holder (fsm_id + migrated_at written by `praxia loop migrate`).
# Path: .praxia/loop_state.toml (relative to project root)
#
# Re-seeded 2026-08-08: the prior seed (task_id="260623_e2-run", epic #2629 under parent #2605)
# was never migrated and had gone stale -- #2605 is backlog status=completed and the epic it
# descends from (#2174) is status=archived, confirmed via `praxia backlog staleness-scan --ids 2629,2605`.
# Migrating that dead premise into the DB would have resumed the loop against already-shipped work.
# The old seed's history is preserved in git (this file was already tracked).

[loop]
version     = "1.0"
task_id     = "260807_autonomous-dev-loop"
current_phase = "TRIAGE"
last_subphase = ""
last_artifact_path = ""
iteration_count = 0
last_activation = "2026-08-08T01:47:52Z"
autonomy_level = "L2"
fsm_id = "autoloop-260807_autonomous-dev-loop"
migrated_at = "2026-08-08T01:49:04.222717296+00:00"

[loop.lock]
token = ""
expiry_ts = ""
timeout_minutes = 45

[loop.gates]
spec_confirmed = false
spec_path = ""
sprint_approved = false
sprint_plan_path = ""

[loop.metrics]
backlog_items_closed_per_iteration = 0
consecutive_zero_progress_iterations = 0
max_zero_progress = 3
last_items_closed = 0
remediate_attempt = 0

[loop.popperian]
max_cost_usd = 25.0
max_walltime_seconds = 3600
max_rewinds = 5

[loop.fingerprint]
recent_tool_hashes = []
stall_threshold = 3
