---
session_id: f8df1d69
topic: Re-run (v2): refine and enrich the xtrax systematic code-audit framework spec, carrying forward the prior converged structure as lineage and integrating new concrete tooling grounding from the xtrax NotebookLM notebook. Resolve the new sub-questions the grounding raises (complexity/coupling as a dimension; mutation testing scope; judgment-track reliability via Refute-or-Promote + empirical oracle + cross-model-critic approximation; tombstone dedup; type-ratchet mechanism choice; doc-coverage gating) and emit a complete, decomposable spec for a backlog DAG + sprint.
task_type: architectural
winner: The enriched xtrax Audit Framework: the settled SHAPE-D unified-dimension structure now bound to EIGHT dimensions and concrete tools. (1) Correctness/bug-surface — jaxlint JL counts + hypothesis metamorphic property tests + checkify/double-where zero-input invariant; rubric: oracle. (2) JAX-purity & tracing — assert_not_tracing guard gate at every transform boundary + batch-dict contract + jaxlint JL001-012 ratchet. (3) Type-hardening — install_import_hook(beartype) in conftest; ratchet = whole-baseline annotation-coverage% + shape-specificity% PRIMARY, AddedTypesTest LibCST diff-gate ENHANCEMENT (fetch-depth:0 + loud-skip fallback); foundation: no-`from __future__ import annotations` guard + ruff F722/F821 per-file ignores. (4) Performance & recompilation — trace/recompile COUNT assertions blocking; wall-time benchmark medians recorded-not-blocked. (5) Documentation — jaxlint JD/JM + ruff D/D402 + interrogate metering coverage% for ratchet; judgment: reviewer LLM-judge on diff-modified files fed by RubricScorer (threshold 4 / 0.75). (6) API ergonomics — surface metrics + ≤4-5 param heuristic; rubric: code-architecture-advisor 1-5 fixed anchors. (7) Test rigor & coverage — pytest-cov 90%+branch + public-symbol contract-test registry diff; scoped+recorded-only mutation (mutmut on safety/+sparse/, nightly) as observation; rubric: test-designer. (8) Structure & Complexity — complexipy + ruff C901/PLR0912/PLR0915, best-ever-per-file ratchet, ceiling only for net-new. FOUNDATION GATES (universal invariants, outside the dimension model): import-linter cycle-block + the no-__future__ guard. JUDGMENT RELIABILITY: Refute-or-Promote on all findings (emit as `observation`), persona-divergence as cheap filter with the all-Claude ceiling admitted in spec prose, empirical-oracle failing-pytest as the acceptance bar for the `bug` LABEL only (pull-based, budget-capped), routing key = executable-as-a-failing-test. DEDUP: tombstone ledger keyed on structural finding_id with content-hash fallback (fuzzy rename layer deferred). ROUTING: CC5 severity×track matrix → two-tier (observations→found-issues.md, ≥threshold→backlog). All prior cross-cutting mechanisms (contract-tested emit envelope, baseline-JSON ratchet, bootstrap+incremental-ruff-select, CI-agent boundary, 5 pre-mortem mitigations) carried forward intact.
created_at: 2026-06-11T13:45:04.921256+00:00
---

# Brainstorm: Re-run (v2): refine and enrich the xtrax systematic code-audit framework spec, carrying forward the prior converged structure as lineage and integrating new concrete tooling grounding from the xtrax NotebookLM notebook. Resolve the new sub-questions the grounding raises (complexity/coupling as a dimension; mutation testing scope; judgment-track reliability via Refute-or-Promote + empirical oracle + cross-model-critic approximation; tombstone dedup; type-ratchet mechanism choice; doc-coverage gating) and emit a complete, decomposable spec for a backlog DAG + sprint.

## Problem Frame
FIXED (unchanged from lineage): locked stack (Python 3.13, JAX/Equinox/optax/orbax/grain, uv, ruff, ty, pytest+cov+benchmark, jaxtyping+beartype, jaxlint MCP); emit into existing praxia substrate (.praxia/audits.jsonl, backlog, dw compose-sprint TOML, .claude/workflows JS) — no parallel system; re-runnable + CI-wireable, no human-eyeball step; alpha/personal repo so no multi-device cluster gates in CI; judgment dims map to existing roster agents. ALSO FIXED now: the converged structure itself (unified dimension model, two tracks, contract-tested emit envelope, baseline-JSON ratchet with per-metric comparators, bootstrap+tightening, CI-agent boundary, incremental ruff-select enablement) — settled, not re-opened.

NEGOTIABLE in this re-run: whether dimensions number 7 or 8 (complexity/coupling); which concrete tool implements each gate and its exact threshold; whether mutation testing is a blocking gate vs recorded-only; the precise judgment-track reliability protocol (how many Refute-or-Promote rounds, what the empirical oracle runs, how to approximate cross-model critics with an all-Claude roster); the type-ratchet mechanism (diff-gate vs whole-baseline-% vs both); and dedup/tombstone implementation details. Confirm and we proceed to divergence on Q1-Q6 + tool-binding.

## Idea Pool
- [ai] Q1a — NEW 8th dimension "Structure & Complexity": deterministic gate = complexipy (--ratchet, cognitive-complexity max 15) + ruff C901/PLR0912/PLR0915 + import-linter (block dependency cycles, enforce layering contract). Trade-off: clean conceptual home for "shape of the code" signal, but adds a dimension to every emit/baseline/roster mapping and risks cognitive-15 being too strict for JAX kernels with inherent lax.cond/where branching.
- [ai] Q1b — Stay at 7 dimensions: fold complexity metrics into Correctness, fold architectural coupling into API-ergonomics. Trade-off: zero structural churn to the settled dimension model, but complexity and coupling become second-class citizens buried under unrelated thresholds, and their ratchets compete for attention inside a dimension scored on other criteria.
- [ai] Q1c — Hybrid: complexity metrics (complexipy + ruff C901/PLR091x) land inside Test-rigor/Correctness as ratcheted sub-metrics, but import-linter cycle-block becomes a standalone ALWAYS-ON gate not tied to any dimension (like a lint pre-flight). Trade-off: keeps dimension count at 7 while giving cycle-prevention the unconditional hard-fail status it deserves, but creates a "gate outside the dimension model" precedent that the unified model was meant to eliminate.
- [ai] Q1-probe-A — import-linter root configuration: xtrax uses a lazy-import __getattr__ at package root. import-linter resolves contracts by static import graph; runtime __getattr__ lazy loading may make the root appear to import nothing, hiding real cycles OR forcing every submodule into an explicit contracts allowlist. Option: pin import-linter to analyze submodules directly (ignore the lazy root shim) and add a contract that the root __getattr__ targets form no cycle among themselves.
- [ai] Q1-probe-B — cognitive-complexity threshold calibration for JAX: instead of a flat cognitive max 15, set the complexipy ratchet to best-ever-per-file (so kernels with inherent branching are grandfathered at their current value and only forbidden from getting worse), with a hard ceiling (e.g. 25) only for net-new files. Trade-off: avoids false-positive churn on legitimately branchy JIT code while still preventing complexity creep.
- [ai] Q2a — Mutation testing as a BLOCKING gate: mutmut (or cosmic-ray) mutation score floor, ratcheted best-ever, starting lenient (>=60%) and tightening +5%/quarter. Trade-off: directly measures test EFFICACY not just line execution, but mutation runs are minutes-to-hours and would dominate per-commit CI wall-clock on an alpha repo where iteration speed matters most.
- [ai] Q2b — Mutation testing RECORDED-ONLY, out-of-band: run on a schedule (nightly/weekly), never per-commit; the score is emitted as a judgment-track input ("test-suite efficacy observation") rather than a hard gate. Trade-off: zero CI latency cost and still surfaces weak spots, but a recorded-only metric with no gate tends to rot — nobody acts on a number that never blocks.
- [ai] Q2c — SCOPED mutation testing: run mutmut only on safety/ + sparse/ jit-guard code (highest blast-radius, smallest surface). Blocking gate but tiny target set keeps runtime bounded. Trade-off: concentrates mutation rigor where a silent bug is most dangerous and keeps CI fast, but leaves the rest of the codebase covered only by line-coverage; requires a maintained allowlist of "critical paths."
- [ai] Q2-probe — coverage-vs-mutation signal overlap: the framework already mandates a 90% coverage gate. Open question for the critic: does 90% line-coverage already buy ~most of the cheap defect signal, making mutation testing a high-cost low-marginal-information add? Option to defer mutation entirely until coverage plateaus and only then introduce it as the "next tier" of rigor once line-coverage stops finding gaps.
- [ai] Q3a — Refute-or-Promote: every judgment-track finding gets two independent-context dispatches — a creative-assert pass (auditor states the finding) then an adversarial-refute pass (a fresh-context agent tries to kill it). Promote to logged finding only if it survives refutation; otherwise drop. Trade-off: cheap, roster-native, filters confident-but-wrong assertions, but two same-family agents can share the same blind spot, so survival is necessary-not-sufficient evidence.
- [ai] Q3b — Cross-model-critic APPROXIMATION via persona divergence: reuse the existing spec-challenger + spec-defender pair, or run independent-context dispatches with deliberately different personas (auditor asserts; security-auditor/oracle refutes). Different system prompts ≈ different priors. Trade-off: maximizes diversity achievable within an all-Claude roster at no extra infra, but it's still one model family — persona divergence reduces but cannot eliminate shared-hallucination risk.
- [ai] Q3c — MANDATORY empirical oracle: before any correctness/bug finding is ACCEPTED as a 'bug', a reviewer agent must produce a FAILING pytest that reproduces it. One empirical execution trumps any amount of agreement. Findings without a repro are downgraded to 'observation', not 'bug'. Trade-off: grounds the judgment track in executable truth and is hallucination-proof for correctness claims, but repro-before-accept costs real agent time per finding and can't cover non-executable findings (style, naming, doc quality).
- [ai] Q3d — Tiered reliability by finding class: cheap Refute-or-Promote (Q3a) for ALL findings as a first filter; mandatory empirical-oracle repro (Q3c) gated ONLY on findings classified 'correctness'/'safety'; persona-divergent critics (Q3b) reserved for high-severity disputed findings. Trade-off: pays the expensive oracle cost only where executable truth exists and matters, but adds a finding-classification step and three protocol paths to maintain.
- [ai] Q3-probe — unanimity-as-warning heuristic: since same-family critics can share hallucinations, treat UNANIMOUS agreement among same-model critics as a yellow flag rather than green — escalate suspiciously-clean consensus to the empirical oracle instead of auto-promoting. Probe for critic: is this overfitting to a theoretical failure mode, and what's the cost of repro-before-accept on a personal repo where the author can just eyeball the claim?
- [ai] Q4a — Structural anchor finding_id = hash(dim + symbol_qualname + rule_id). Stable, human-legible provenance, cheap to compute. Trade-off: qualname changes under rename/move, so a refactor relogs the "same" finding as new (false re-surfacing) and a moved-but-unfixed issue evades the tombstone. Best for findings tightly bound to a named symbol.
- [ai] Q4b — Tombstone ledger .praxia/audit_tombstones.jsonl recording accepted/wontfix/out-of-scope finding_ids so resolved-but-unfixable findings are never relogged. The emit step diffs new findings against the tombstone set before writing. Trade-off: gives explicit "we decided not to fix this" memory and keeps the active ledger clean, but introduces a second source of truth that must stay in sync and a governance question of who writes tombstones.
- [ai] Q4c — Content-hash variant finding_id = hash(dim + normalized-evidence-snippet) for findings with NO stable symbol (module-level, cross-file, doc-prose). Survives rename but is sensitive to whitespace/reflow unless aggressively normalized. Trade-off: covers the symbol-less findings Q4a can't anchor, but normalization is finicky and a one-character evidence edit can collide-or-split the id.
- [ai] Q4d — Composite id strategy: use Q4a structural anchor when a qualname exists, fall back to Q4c content-hash when it doesn't, and carry BOTH a primary id and a secondary "fuzzy" key so rename-detection can match on the content-hash even when the qualname id drifts. Tombstone ledger (Q4b) keys on the primary id but rename-reconciliation consults the fuzzy key. Trade-off: robust against both rename and reflow, but the dual-key bookkeeping is the most complex option to implement and test.
- [ai] Q5a — Whole-baseline ratchet: two AST metrics — annotation-coverage % (fraction of public params/returns annotated) and shape-specificity % (fraction of array annotations that are jaxtyping shape-typed vs bare Array) — both tracked best-ever in baseline JSON. Trade-off: gives a single burndown number per metric and needs no git-base, but it's a lagging aggregate that doesn't pinpoint WHICH new code added debt, so a clean-overall % can hide a freshly-untyped public function.
- [ai] Q5b — AddedTypesTest diff-gate (LibCST): any newly-added or modified public function in the diff MUST be fully and shape-specifically annotated; the untyped surface can only shrink. Trade-off: stops new debt at the merge boundary precisely where it's introduced, but needs a clean git base to diff against in CI (merge-base resolution), and "modified function" detection via LibCST must handle signature-only vs body-only edits sanely.
- [ai] Q5c — BOTH mechanisms: diff-gate (Q5b) blocks new debt at merge time, whole-baseline-% (Q5a) tracks burndown of pre-existing debt so the legacy untyped surface visibly shrinks over time. Trade-off: complete coverage of both "no new debt" and "old debt declining" with complementary failure modes, but it's two pipelines to build, two thresholds to tune, and the diff-gate's git-base dependency still applies.
- [ai] Q5-enforcement-A — runtime shape-check via jaxtyping install_import_hook in conftest (global test-time): every test import gets beartype shape-validation. Trade-off: catches shape lies anywhere the test suite exercises code, broadest runtime coverage, but the global hook can surprise with import-order sensitivity and slows test collection.
- [ai] Q5-enforcement-B — runtime shape-check via pytest-beartype plugin (per-package opt-in). Trade-off: more explicit and scoped than the global import hook, easier to reason about which packages are validated, but requires per-package config and may miss code paths the plugin's package selection doesn't cover.
- [ai] Q5-setup — jaxtyping foundation tasks: the never-`from __future__ import annotations` constraint must be enforced (a ruff/grep gate that fails if that import appears, since it stringizes annotations and breaks runtime shape-checking), plus ruff per-file ignores for F722 (forward-ref syntax in jaxtyping shape strings) and F821 (undefined-name on shape variables) configured as a precondition task. Trade-off: these are unglamorous setup gates but are load-bearing — without them the entire shape-typing track silently no-ops.
- [ai] Q6a — Docs DETERMINISTIC track: jaxlint JD001-005 + JM001-003 + ruff D rules + ruff D402 + interrogate docstring-coverage gate (>70% ratchet, +5%/quarter). Trade-off: fully mechanical, CI-native, no agent cost, enforces presence/format/structure, but presence != quality — it can't tell a real docstring from a placeholder one-liner.
- [ai] Q6b — Docs JUDGMENT track: LLM-judge scores doc quality 1-5; hard threshold of 4 applied ONLY to files modified in the diff (reviewer agent). Trade-off: catches semantic emptiness/inaccuracy the deterministic track misses, scoped to changed files to bound cost, but 1-5 LLM scores are noisy and the threshold-of-4 boundary will produce disputable near-misses.
- [ai] Q6c — jaxbeans RubricScorer (threshold 0.75) placement: option (i) REPLACE the bespoke LLM-judge with RubricScorer as the single quality engine (one rubric, one threshold, less custom code); option (ii) FEED RubricScorer's structured rubric output INTO the LLM-judge as evidence so the 1-5 score is rubric-grounded. Trade-off: replace = simpler and reuses a tested scorer; feed = richer signal but two-stage and more moving parts.
- [ai] Q6-probe — interrogate vs jaxlint JD001 overlap: both detect docstring PRESENCE. Option (i) treat as redundant and drop interrogate, letting JD001 own presence + getting coverage% as a free byproduct; option (ii) treat as complementary — JD001 gates per-symbol presence in the deterministic pass, interrogate provides the aggregate coverage% number for the ratchet baseline. Probe for critic: is running both just double-reporting the same failures with two different error formats?
- [ai] CC1 — found-issues.md passive ledger vs backlog routing: option (a) keep a flat passive found-issues.md append-only ledger (low ceremony, human-browsable, but rots and never gets scheduled); option (b) route every accepted finding directly into the praxia backlog DAG as a task node (gets sprint-scheduled, but heavier and may flood the backlog with low-priority lint nits); option (c) two-tier — observations land in found-issues.md, only findings above a severity threshold auto-route to backlog. Trade-off: ceremony vs actionability.
- [ai] CC2 — checkify + double-where zero-input invariant as a concrete JAX-purity contract test under Correctness: assert that masked/zero-input branches don't produce NaN-gradients (the classic double-where pattern) and wrap critical kernels in jax.experimental.checkify to surface silent NaN/inf/div-by-zero at test time. Trade-off: encodes a real JAX foot-gun as an executable gate, but checkify changes function signatures (returns an error obj) so it needs a test-only wrapper, not production instrumentation.
- [ai] CC3 — metamorphic-relation property tests (hypothesis) as a Correctness gate: encode known invariants of the transforms (e.g. permutation-equivariance, scale-covariance, idempotence of a normalization) as hypothesis-driven metamorphic relations rather than fixed golden values. Trade-off: catches whole classes of bugs that example-based tests miss and needs no oracle output, but writing good metamorphic relations for JAX numerics is skilled work and flaky-shrink behavior under hypothesis can be noisy.
- [ai] CC4 — jaxtyping setup as an explicit FOUNDATION task in the DAG (prerequisite to Q5/Q6 tracks): ruff per-file-ignores for F722 on modules using shape strings, F821 handling for shape variables, and the no-`from __future__ import annotations` guard, all landed as task-zero before any type-ratchet or shape-check gate is enabled. Trade-off: makes the dependency explicit in the backlog DAG so the type track can't be scheduled before its substrate exists, at the cost of an unglamorous blocking node early in the sprint.
- [ai] CC5 — severity x track routing matrix as a cross-cutting emit-layer concern: define how (deterministic|judgment) track AND (info|minor|major|critical) severity jointly determine destination — block CI / tombstone-eligible / found-issues.md / backlog node. One small table that every dimension's findings flow through, decoupling "how a finding is detected" from "what happens to it." Trade-off: clean separation of detection from disposition, but it's a new shared schema all eight (or seven) dimensions must conform to.
- [user] Idea space is well-populated (33 options across Q1-Q6 + cross-cutting, 5 tensions surfaced). Ready to converge. My leading picks: Q1 → 8th dimension "Structure & Complexity" (0) but with import-linter cycle-block as its gate (resolve the standalone-gate tension by giving it a home); Q2 → recorded-only/scoped (6+7), not blocking; Q3 → tiered (12): Refute-or-Promote on all + empirical oracle on correctness/safety + downgrade-to-observation without repro; Q4 → composite qualname+fuzzy-content with tombstone ledger (17+15); Q5 → BOTH diff-gate + baseline-% (20); Q6 → deterministic interrogate+ruff-D + judgment LLM-judge fed by RubricScorer (24+25+26). Let's converge.

## Decision Log
- [REJECT] Q3c empirical-oracle repro as MANDATORY before accepting any correctness/safety finding: [CRITIC] idea=Q3c lens=cost-runnability finding=Mandatory repro-before-accept on every correctness/safety finding re-enters lineage pre-mortem risk #2 (judgment-track-never-ran) through the back door; on a solo alpha repo the per-finding agent cost makes the track too heavy to run. severity=FATAL. REFRAME (not delete): keep the PRINCIPLE — executable truth trumps say-so — but make repro the acceptance bar for the `bug` LABEL, not a precondition for LOGGING. Cheap Refute-or-Promote always runs and always emits a finding as `observation`. Promotion observation->bug requires a failing pytest, but that promotion is PULL-BASED and budget-capped (author/scheduled tier elects which observations to promote). Track stays runnable (cheap tier always produces output); strong `bug` claim still requires executable ground truth. This is the minimal protocol that beats one-agent-say-so without making the track never run.
- [MERGE] Idea 0 — 8th dimension "Structure & Complexity" with import-linter cycle-block AS its gate: [CRITIC] idea=0 lens=dimension-earn finding=Idea 0 conflates a RATCHETABLE METRIC (complexity) with a UNIVERSAL INVARIANT (no import cycles); the latter does not belong inside a dimension. severity=MAJOR. Cycle-prevention is not a scored axis — you do not ratchet cycle-count toward zero, you forbid cycles full stop — exactly like the no-`from __future__ import annotations` guard (idea 23/31) the frame ALREADY sanctions as a load-bearing structural gate OUTSIDE any dimension. So the "no gate outside a dimension" rule is already broken by accepted foundation gates; import-linter cycle-block joins THAT foundation class. complexipy + ruff C901/PLR091x IS genuinely a scored, best-ever-per-file ratchet (idea 4) and earns dimensional status. RESOLUTION: 8th dimension = complexity ONLY (complexipy+C901, best-ever-per-file per idea 4); import-linter cycle-block = foundation gate alongside jaxtyping guards, NOT this dimension's gate. Merge ideas 0+2+4+31 along this seam.
- [MERGE] Q3d tiered reliability routed by finding-CLASS (correctness/safety classification): [CRITIC] idea=12 lens=classifier-error finding=Routing to the oracle tier by classifying a finding `correctness/safety` requires a classifier that is itself error-prone — a JAX-purity finding (missing assert_not_tracing) is BOTH correctness AND structural, and a misclassification silently downgrades a real bug past the oracle. severity=MAJOR. Tiering by finding-class adds MORE failure surface than it removes. FIX: replace the ambiguous semantic key with an OPERATIONAL one — "is this finding executable-as-a-failing-test?" That is near-binary and low-ambiguity: either a red pytest exists or it does not. Style/naming/doc are non-executable by construction; correctness/safety/purity are executable by construction. The assert_not_tracing case IS executable (trace the fn, assert the guard fires) so it routes correctly with ZERO classification judgment. Collapses 3 protocol paths to 2 and deletes the classifier failure surface. Accept the TIERING, reject the routing key.
- [MERGE] Ideas 5-9 mutation testing scope + idea 28 interrogate/JD001 redundancy: [CRITIC] idea=6+7,27 lens=marginal-signal finding=With a 90% branch-coverage gate already mandated, mutation's marginal signal is real but concentrated, and running BOTH interrogate+JD001 as GATES is double-reporting presence. severity=MINOR. (a) Mutation: idea 9 (defer entirely) OVER-cuts — coverage proves execution, mutation proves assertion-strength, and those diverge precisely in safety/+sparse/ jit-guard code where a silently-passing test is most dangerous. Blocking mutation (idea 5) is FATAL for CI wall-clock. Survivor = merge 6+7: SCOPED to safety/+sparse/ AND recorded-only/out-of-band (nightly), never per-commit, emitted as a judgment-track observation. Not a gate, not deleted. (b) interrogate vs JD001: running both as gates is ceremony, but idea 27 option (ii) is NOT redundant — JD001 GATES per-symbol presence, interrogate METERS aggregate coverage-% for the ratchet baseline only. One gates, one meters. Reject interrogate-as-second-presence-gate; keep it as the coverage-% meter.
- [REJECT] Idea 14 unanimity-as-warning + idea 11 persona-divergence on an all-Claude roster: [CRITIC] idea=14 lens=overfit finding=Unanimity-as-warning is theoretical overfit on a monoculture roster — escalating "suspiciously clean" same-family consensus routes to MORE of the same family, which cannot produce independent signal; it is a branch that feels rigorous but resolves to the same blind spot. severity=MAJOR (reject idea 14). Persona-divergence (idea 11) reduces but provably cannot eliminate shared hallucination — one model family. The spec should ADMIT the ceiling, not engineer theater around it: the ONLY hallucination-proof check available is the empirical oracle (executable repro). KEEP idea 11 as a labeled CHEAP first-filter (free, reuses existing challenger/defender agents) but the spec must STATE: "same-family critics surviving refutation is necessary-not-sufficient; the only ground truth is a failing test." Reject 14 outright; accept 11 only with its admitted ceiling written into the spec.
- [MERGE] Idea 20 type-ratchet: diff-gate (Q5b) as primary vs whole-baseline-% (Q5a) as primary: [CRITIC] idea=20 lens=git-base finding=The diff-gate's merge-base dependency is brittle as a PRIMARY gate — GitHub Actions' default fetch-depth:1 (shallow clone) breaks `git merge-base` outright, after which the gate either errors or silently passes (silent no-op = the worst failure for a gate). severity=MAJOR. Accept BOTH mechanisms (idea 20) but INVERT primacy: whole-baseline-% (Q5a) is PRIMARY (no git-base, always computable, cannot silently no-op, gives a burndown number); diff-gate (Q5b) is the ENHANCEMENT that runs only when merge-base resolves. Add a foundation requirement: configure fetch-depth:0 (idea 31 foundation class) AND an explicit "skip-with-loud-warning, never pass-silently" fallback when merge-base is unavailable. This makes the ratchet robust on solo/shallow CI topology while keeping the precise no-new-debt boundary when it is available.
- [REJECT] Ceremony-exceeding-signal cut list for an alpha repo: [CRITIC] lens=ceremony-on-alpha finding=Two items exceed signal on a solo alpha repo and should be CUT/deferred. severity=MINOR. (1) Idea 14 unanimity-as-warning — already rejected, it is the canonical ceremony item (a rigor-flavored branch that cannot produce independent signal on a monoculture roster). (2) Idea 17's DUAL-KEY (primary qualname + secondary fuzzy-content + rename-reconciliation) — full dual-key bookkeeping is the most complex option in the pool and rename-reconciliation logic is hard to test; on an alpha repo START with idea 14-of-Q4 (15: tombstone ledger) keyed on the SIMPLE structural id (Q4a/14) plus content-hash fallback (Q4c/16) ONLY for symbol-less findings — defer the fuzzy rename-reconciliation layer until churn actually proves false-resurfacing is a real problem. Accept tombstone ledger (15); accept structural-id-with-content-fallback (14+16 merge); DEFER the rename-reconciliation fuzzy-key layer of 17 as premature. CC5 severity x track matrix (33), CC2 checkify/double-where (30), CC3 metamorphic (31) all EARN their place — keep.
- [MERGE] Q1: 8 dimensions. The 8th "Structure & Complexity" is COMPLEXITY-ONLY (complexipy + ruff C901/PLR0912/PLR0915, best-ever-per-file ratchet, hard ceiling only for net-new files). import-linter cycle-block is NOT this dimension's gate — it becomes a FOUNDATION gate (universal invariant alongside the no-`from __future__ import annotations` guard), not a scored axis.: Resolves the idea-0 standalone-gate tension by recognizing it conflated a ratchet (complexity, which legitimately scores per-dimension) with an invariant (cycle-prevention, which is universal and belongs at foundation). best-ever-per-file ratchet grandfathers legitimately-branchy JAX kernels while preventing creep; flat cognitive-15 would false-positive on lax.cond/where code.
- [MERGE] Q3: minimal-but-credible reliability protocol. (1) Refute-or-Promote on ALL judgment findings, always runs, emits as `observation`. (2) Persona-divergence as a labeled cheap filter WITH the all-Claude ceiling written into the spec prose ("survival is necessary-not-sufficient; only a failing test is ground truth"). (3) Empirical oracle = acceptance bar for the `bug` LABEL (promotion observation→bug requires a failing pytest), PULL-BASED + budget-capped, NOT a mandatory precondition for logging. Routing key = "is this finding executable-as-a-failing-test?" (operational, near-binary), NOT the error-prone correctness/safety classifier. Reject unanimity-as-warning.: Mandatory repro-before-accept (Q3c framing) re-enters lineage pre-mortem risk #2 (judgment-track-never-ran) by making the track too heavy on a solo repo. Making the empirical oracle the bar for the BUG LABEL rather than a logging precondition keeps the cheap tier always-runnable while still beating one-agent-say-so. Re-keying to executability deletes the ambiguous classifier the forcing-beat correctly flagged (JAX-purity findings are both correctness and structural). Unanimity-as-warning is ceremony on a monoculture roster.
- [MERGE] Q2 mutation: scoped to safety/ + sparse/ jit-guard code AND recorded-only/out-of-band (nightly), emitted as a judgment-track observation — NOT a blocking per-commit gate, NOT deferred entirely. Q5 type-ratchet: BOTH mechanisms but invert primacy — whole-baseline annotation-coverage% + shape-specificity% is PRIMARY (no git-base, can't silently no-op), AddedTypesTest diff-gate is the ENHANCEMENT (runs when merge-base resolves; CI uses fetch-depth:0 + loud-skip-never-pass-silently fallback). Enforcement = install_import_hook(beartype) in conftest. Q6 docs: deterministic = jaxlint JD/JM + ruff D + JD001 per-symbol presence, interrogate METERS coverage% for the ratchet baseline only (not a second gate); judgment = LLM-judge on diff-modified files FED BY RubricScorer (feed, not replace).: Blocking mutation dominates CI wall-clock on an alpha repo (reject 5); recorded+scoped keeps the efficacy signal where a silent bug is most dangerous without taxing iteration. Baseline-%-primary avoids the diff-gate's git-base fragility silently no-opping the whole type track. interrogate-as-meter (not second gate) removes JD001 double-reporting. RubricScorer feeds the judge so the 1-5 score is rubric-grounded rather than vibes.
- [ACCEPT] Q4 dedup: tombstone ledger (.praxia/audit_tombstones.jsonl) keyed on a structural finding_id = hash(dim + symbol_qualname + rule_id), with content-hash hash(dim + normalized-evidence) FALLBACK for symbol-less findings. DEFER the fuzzy rename-reconciliation dual-key layer until churn proves false-resurfacing is real. CC1 disposition: two-tier — observations land in found-issues.md, only findings at/above a severity threshold auto-route to praxia backlog; CC5 severity×track routing matrix (audit/routing.toml) is the shared schema mapping (track × severity) → destination {block-CI | tombstone-eligible | found-issues.md | backlog-node}. Keep CC2 (checkify/double-where zero-input NaN-gradient contract test) and CC3 (hypothesis metamorphic-relation property tests) as concrete Correctness/JAX-purity gates.: Structural id is human-legible and cheap; content-hash fallback covers module-level/doc findings; the dual-key fuzzy layer is premature complexity on an alpha repo. Two-tier CC1 gives CC5's matrix a concrete destination set (the critic's flagged residual) and prevents backlog flood while keeping a browsable ledger. CC2/CC3 encode known JAX foot-guns as executable gates.

## Assumptions

## TBDs

## Pre-mortem Record
**User:** Six-months-failed pre-mortem for the enriched v2 design — NEW death modes beyond the lineage's five: (6) FOUNDATION-NODE STARVATION — the jaxtyping setup foundation (no-__future__ guard, F722/F821 ignores, import-linter root config) was deprioritized as "unglamorous plumbing," never landed, so the Type-hardening and Structure dimensions could never be enabled and silently stayed dark for months; the DAG must make these foundation tasks hard blockers with nothing schedulable above them until green. (7) install_import_hook IMPORT-ORDER FRAGILITY — the global beartype hook in conftest interacted badly with xtrax's lazy __getattr__ root and orbax/grain import side-effects, making test collection flaky; we ripped the hook out to stop the flakiness and lost all runtime shape-checking. Mitigation: pin the hook to xtrax submodules (not the lazy root), and have a smoke test that asserts the hook is actually active (a deliberately-wrong shape must raise) so silent removal is itself caught. (8) BEST-EVER-PER-FILE RATCHET LOCK-IN — a fluke-good complexipy/coverage measurement on a file became its permanent floor, and a later legitimate refactor that briefly raised complexity was blocked, so we disabled the ratchet for that file and it never came back. Mitigation: best-ever floors need an explicit `--accept` escape hatch with a recorded rationale, and the ratchet compares against a committed value a human approved, not the literal best-ever observed. (9) RUBRICSCORER/LLM-JUDGE DOUBLE-COUNTING — feeding RubricScorer into the LLM-judge produced correlated scores (the judge just echoed the scorer), so the "two-stage" doc-quality signal was really one signal wearing two hats, and a genuinely-bad docstring that the scorer rated structurally-complete sailed through. Mitigation: the judge must score a DIFFERENT axis (semantic accuracy/usefulness) than the scorer (structural completeness), and the spec must name which axis each owns. (10) TOMBSTONE GOVERNANCE DRIFT — tombstones accumulated as a graveyard of "wontfix" that nobody reviewed, and a real regression got auto-suppressed because it hashed to a tombstoned finding_id after an unrelated edit; the tombstone ledger needs an expiry/review cadence and must NOT suppress a finding whose severity increased since tombstoning. These five join the lineage's five as the full risk table. Ready for synthesis.
**AI:** _not recorded_

## Acceptance Criteria
**Given** FIXED (unchanged from lineage): locked stack (Python 3.13, JAX/Equinox/optax/orbax/grain, uv, ruff, ty, pytest+cov+benchmark, jaxtyping+beartype, jaxlint MCP); emit into existing praxia substrate (.praxia/audits.jsonl, backlog, dw compose-sprint TOML, .claude/workflows JS) — no parallel system; re-runnable + CI-wireable, no human-eyeball step; alpha/personal repo so no multi-device cluster gates in CI; judgment dims map to existing roster agents. ALSO FIXED now: the converged structure itself (unified dimension model, two tracks, contract-tested emit envelope, baseline-JSON ratchet with per-metric comparators, bootstrap+tightening, CI-agent boundary, incremental ruff-select enablement) — settled, not re-opened.

NEGOTIABLE in this re-run: whether dimensions number 7 or 8 (complexity/coupling); which concrete tool implements each gate and its exact threshold; whether mutation testing is a blocking gate vs recorded-only; the precise judgment-track reliability protocol (how many Refute-or-Promote rounds, what the empirical oracle runs, how to approximate cross-model critics with an all-Claude roster); the type-ratchet mechanism (diff-gate vs whole-baseline-% vs both); and dedup/tombstone implementation details. Confirm and we proceed to divergence on Q1-Q6 + tool-binding.
**When** implementing The enriched xtrax Audit Framework: the settled SHAPE-D unified-dimension structure now bound to EIGHT dimensions and concrete tools. (1) Correctness/bug-surface — jaxlint JL counts + hypothesis metamorphic property tests + checkify/double-where zero-input invariant; rubric: oracle. (2) JAX-purity & tracing — assert_not_tracing guard gate at every transform boundary + batch-dict contract + jaxlint JL001-012 ratchet. (3) Type-hardening — install_import_hook(beartype) in conftest; ratchet = whole-baseline annotation-coverage% + shape-specificity% PRIMARY, AddedTypesTest LibCST diff-gate ENHANCEMENT (fetch-depth:0 + loud-skip fallback); foundation: no-`from __future__ import annotations` guard + ruff F722/F821 per-file ignores. (4) Performance & recompilation — trace/recompile COUNT assertions blocking; wall-time benchmark medians recorded-not-blocked. (5) Documentation — jaxlint JD/JM + ruff D/D402 + interrogate metering coverage% for ratchet; judgment: reviewer LLM-judge on diff-modified files fed by RubricScorer (threshold 4 / 0.75). (6) API ergonomics — surface metrics + ≤4-5 param heuristic; rubric: code-architecture-advisor 1-5 fixed anchors. (7) Test rigor & coverage — pytest-cov 90%+branch + public-symbol contract-test registry diff; scoped+recorded-only mutation (mutmut on safety/+sparse/, nightly) as observation; rubric: test-designer. (8) Structure & Complexity — complexipy + ruff C901/PLR0912/PLR0915, best-ever-per-file ratchet, ceiling only for net-new. FOUNDATION GATES (universal invariants, outside the dimension model): import-linter cycle-block + the no-__future__ guard. JUDGMENT RELIABILITY: Refute-or-Promote on all findings (emit as `observation`), persona-divergence as cheap filter with the all-Claude ceiling admitted in spec prose, empirical-oracle failing-pytest as the acceptance bar for the `bug` LABEL only (pull-based, budget-capped), routing key = executable-as-a-failing-test. DEDUP: tombstone ledger keyed on structural finding_id with content-hash fallback (fuzzy rename layer deferred). ROUTING: CC5 severity×track matrix → two-tier (observations→found-issues.md, ≥threshold→backlog). All prior cross-cutting mechanisms (contract-tested emit envelope, baseline-JSON ratchet, bootstrap+incremental-ruff-select, CI-agent boundary, 5 pre-mortem mitigations) carried forward intact.
**Then**
  - [ ] _add specific measurable criteria_
