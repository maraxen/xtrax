---
session_id: c21cc4e2
topic: Backlog #4203 (xtrax) CORRECTED RE-RUN of session 7d98aa59: thread GW-02's 7 kwargs (frozen_context, current_config, repo, ratchet_ref_name, commit_tree_sha, callable_name, concrete_inputs) PLUS a required 4th piece (candidate_target_path) from run_campaign_loop through run_multi_iteration_loop into run_one_candidate_pass; solve fitness carry-forward; design the commit_tree_sha computation seam.
task_type: architectural
winner: Composite design (4 required pieces, carried forward from session 7d98aa59 with the pre-mortem mitigation now folded in from the start): (a) Flat-kwargs threading -- add the 7 GW-02 kwargs (frozen_context, current_config, repo, ratchet_ref_name, callable_name, concrete_inputs, plus commit_tree_sha as now-optional) as new keyword params on both run_multi_iteration_loop and run_campaign_loop, forwarded unchanged layer to layer, matching the existing flat-kwarg convention. (b) Fitness carry-forward -- add fitness_dict: dict[str, float] as a new additive field on OneCandidatePassResult (populated alongside ratchet_decision); run_multi_iteration_loop keeps loop-local best_fitness/best_higher_is_better state, None for iteration 1 (triggers GW-02's existing automatic-accept sentinel), updated to result.fitness_dict ONLY when result.accepted is True, left unchanged on reject. (c) commit_tree_sha computation -- relax run_one_candidate_pass's commit_tree_sha to str | None = None (genuinely additive: existing callers passing a literal string are unaffected) and add a new commit_tree_sha_fn: Callable[[CandidateHandoff, Path, Path, str | None], str] = compute_candidate_tree_sha injection seam (same DI convention as guarded_evaluate_fn), invoked inside run_one_candidate_pass right after step 1 (handoff = dispatch_backend.dispatch_candidate()) when commit_tree_sha is None. The real default computes a whole-repo-tree substitution (base = current best-so-far tree via read_best_so_far, one path's blob replaced with the candidate's content) using a scratch GIT_INDEX_FILE (git read-tree + git update-index --add --cacheinfo + git write-tree), never a synthetic single-file tree -- required because reset_worktree_to_best_so_far does a real git reset --hard onto this lineage. (d) candidate_target_path: Path -- a new REQUIRED (no default) keyword parameter on run_one_candidate_pass, threaded flat from run_campaign_loop -> run_multi_iteration_loop -> run_one_candidate_pass -> commit_tree_sha_fn. The repo-relative destination path for the candidate's content inside the tree substitution; must be explicit and caller-supplied, never inferred from handoff.path's basename, because create_pending_commit/advance_best_so_far never validate tree semantics -- a guessed path would silently corrupt best-so-far git lineage with no exception and no failing test, exactly the risk this required parameter closes (omitting it raises TypeError immediately, fail-fast instead of silent corruption). UNLIKE commit_tree_sha's relaxation, this is NOT backward-compatible for existing direct-call test sites in PR #90's suite that construct a full run_one_candidate_pass call -- accepted as a named, scoped mechanical migration cost (updating those call sites), not a design flaw; a default-with-fallback was explicitly rejected because it would reintroduce the exact silent-inference failure mode this parameter exists to close. Explicitly documented scope boundary: does not support process-level crash-resume of loop-local fitness state (no existing resume entry point exists to attach recovery to; deferred to a future item).
created_at: 2026-08-12T15:05:12.058186+00:00
---

# Brainstorm: Backlog #4203 (xtrax) CORRECTED RE-RUN of session 7d98aa59: thread GW-02's 7 kwargs (frozen_context, current_config, repo, ratchet_ref_name, commit_tree_sha, callable_name, concrete_inputs) PLUS a required 4th piece (candidate_target_path) from run_campaign_loop through run_multi_iteration_loop into run_one_candidate_pass; solve fitness carry-forward; design the commit_tree_sha computation seam.

## Problem Frame
Fixed constraints (carried forward from 7d98aa59, unchanged): (1) run_one_candidate_pass's existing GW-02 signature/behavior stays additive-only -- no breaking changes to existing callers passing literal commit_tree_sha strings. (2) compute_ratchet_decision's signature is locked. (3) create_pending_commit requires a real git TREE object sha. (4) The file's existing Callable[..., X]=real_default injection-seam convention governs any new computed-value seam. (5) run_multi_iteration_loop's per-candidate loop structure stays. (6) Zero retry logic. (7) No backward-compat requirement for run_multi_iteration_loop/run_campaign_loop's own callers.

Additionally fixed (established as findings in 7d98aa59, not re-open for debate here): the commit_tree_sha_fn seam must live INSIDE run_one_candidate_pass (not one level up) because handoff.path doesn't exist until dispatch runs inside that function; commit_tree_sha_fn must produce a whole-repo-tree substitution, never a synthetic single-file tree; best_fitness carry-forward is loop-local Python state (not git-notes), updated only on accept.

Negotiable in THIS session: only the exact wording/shape of the winner text and the concrete Acceptance Criteria bullets -- the substantive design itself is not being re-decided.

## Idea Pool
- [user] Carrying forward 7d98aa59's composite as the primary idea, plus two real placement alternatives for the NEW candidate_target_path piece specifically (this is the one genuinely open question in this session):
- [user] Primary (carried forward): flat-kwargs threading of the 7 GW-02 kwargs + candidate_target_path through run_campaign_loop -> run_multi_iteration_loop -> run_one_candidate_pass; additive OneCandidatePassResult.fitness_dict field + loop-local best_fitness carry-forward (updated only on accept); commit_tree_sha relaxed to optional + commit_tree_sha_fn injection seam inside run_one_candidate_pass performing whole-repo-tree substitution at candidate_target_path.
- [user] Alt-1: candidate_target_path as a new field on CandidateHandoff itself (controller/dispatch.py, LC-03) rather than a separate parameter threaded through the loop functions -- the dispatch backend, which stages the file, plausibly already knows its intended repo-relative destination. Rejected on inspection: CandidateHandoff is a separate, already-independently-owned module (LC-03) outside this ticket's file set; modifying its dataclass shape would require touching every existing DispatchBackend implementation (including MockDispatchBackend) and is a larger blast radius than one new keyword parameter on functions already being changed by this ticket.
- [user] Alt-2: candidate_target_path bundled into a two-field tuple/small dataclass with commit_tree_sha (since they're both git-substitution-shaped) rather than a fully separate flat parameter. Rejected: violates the flat-kwargs convention decision already locked in 7d98aa59 (D was rejected for the same reason at the 7-kwarg level); introducing a bundle for just these two would be an inconsistent one-off.
- [user] Both alternatives inspected and rejected on their merits, not rubber-stamped -- confirming the primary's flat, separate, required-parameter placement for candidate_target_path is the right call.
- [user] One more genuine idea, on the AC-authoring gap specifically (the second thing this corrective session must fix): what SHOULD count as measurable Acceptance Criteria for this design, restated concretely rather than left as prose -- (a) fitness_dict round-trips byte-identically from guarded_evaluate_fn onto OneCandidatePassResult, (b) best_fitness updates to the accepted candidate's fitness_dict on accept and stays unchanged on reject (verified via spy on consecutive run_one_candidate_pass calls), (c) commit_tree_sha_fn's default produces a tree containing every base-tree path unchanged except candidate_target_path, never a single-file tree, (d) omitting candidate_target_path raises TypeError before any dispatch/git call, (e) all 8 threaded values (7 GW-02 kwargs + candidate_target_path) survive run_campaign_loop -> run_multi_iteration_loop -> run_one_candidate_pass unmutated across at least 2 iterations. This is ready to converge -- divergence exhausted, both the substantive design (carried forward, confirmed correct against 2 rejected alternatives) and the AC-authoring gap (5 concrete criteria drafted) are covered.

## Decision Log
- [REJECT] Alt-1: candidate_target_path as a new field on CandidateHandoff (controller/dispatch.py, LC-03): CandidateHandoff is a separate, independently-owned module (LC-03) outside this ticket's file set. Modifying its dataclass shape would require touching every existing DispatchBackend implementation (including MockDispatchBackend), a larger blast radius than one new keyword parameter on functions already being changed by this ticket.
- [REJECT] Alt-2: bundle candidate_target_path + commit_tree_sha into a small tuple/dataclass: Violates the flat-kwargs convention already locked in 7d98aa59 (idea D rejected for the same reason at the 7-kwarg level); a bundle for just these two would be an inconsistent one-off against the file family's dominant convention.
- [REJECT] Give candidate_target_path a default (e.g. None) with an inferred fallback, instead of making it strictly required: Defeats the entire purpose of the parameter: the original pre-mortem's failure mode WAS silent inference from handoff.path producing a wrong/missing blob substitution. A default-with-fallback reintroduces that exact silent-corruption risk. Accept the mechanical cost instead: candidate_target_path is required, no default, and existing direct-call test sites in PR #90's suite will need updating -- a named, scoped migration cost, not a design flaw.

## Assumptions

## TBDs

## Pre-mortem Record
**User:** Most likely failure mode 6 months out: the "named, scoped mechanical migration cost" of adding candidate_target_path as a new required parameter turns out to be much larger than "update a few direct call sites in PR #90's test suite." If run_one_candidate_pass has been used as a public, stable building block by OTHER code written after PR #90 merged (a second controller entrypoint, an ad-hoc script, a notebook-style experiment harness) that the original ticket's file-scoped review never surfaced, every one of those callers breaks with a TypeError the moment this change ships -- not caught by the scoped `controller/ -k "main_loop or evaluate_adapter or multi_iteration"` test selection this whole GW-02/#4203 lineage has used throughout (deliberately narrow, per local-compute-limits.md), because a caller outside that directory/keyword filter never gets exercised by the verification command. The "additive, non-breaking" framing for pieces (a)-(c) lulls the review into treating the WHOLE composite as low-risk, when piece (d) alone is a genuine breaking API change to an already-merged, already-relied-upon function -- and nothing in this design proposes actually grepping the full repo for existing run_one_candidate_pass call sites before shipping, only updating "PR #90's test suite," which is an assumption about the blast radius, not a verified fact.
**AI:** Verified directly, not assumed: `grep -rn "run_one_candidate_pass(" --include="*.py" .` (repo root, excluding `.venv` and the function's own `def` line) run 2026-08-12 finds exactly ONE production call site -- `controller/multi_iteration_loop.py:390` -- plus 28 direct-call sites, all inside `tests/controller/test_main_loop.py` (Track B's own test suite from PR #90). No caller exists anywhere else in this repo: no second controller entrypoint, no ad-hoc script, no notebook harness. The pre-mortem's feared scenario (a hidden external caller written after PR #90 merged, invisible to the scoped `-k` test selection) is NOT realized here -- this grep is repo-wide (not directory- or keyword-scoped), so it would have caught any such caller regardless of test naming. Mitigation folded into Acceptance Criteria bullet 8 below: this same grep must be re-run immediately before `candidate_target_path` ships (not just once now, in case new callers land between spec and implementation), and its result reported in the fixer's own verification output, not assumed.

## Acceptance Criteria
**Given** Fixed constraints (carried forward from 7d98aa59, unchanged): (1) run_one_candidate_pass's existing GW-02 signature/behavior stays additive-only -- no breaking changes to existing callers passing literal commit_tree_sha strings. (2) compute_ratchet_decision's signature is locked. (3) create_pending_commit requires a real git TREE object sha. (4) The file's existing Callable[..., X]=real_default injection-seam convention governs any new computed-value seam. (5) run_multi_iteration_loop's per-candidate loop structure stays. (6) Zero retry logic. (7) No backward-compat requirement for run_multi_iteration_loop/run_campaign_loop's own callers.

Additionally fixed (established as findings in 7d98aa59, not re-open for debate here): the commit_tree_sha_fn seam must live INSIDE run_one_candidate_pass (not one level up) because handoff.path doesn't exist until dispatch runs inside that function; commit_tree_sha_fn must produce a whole-repo-tree substitution, never a synthetic single-file tree; best_fitness carry-forward is loop-local Python state (not git-notes), updated only on accept.

Negotiable in THIS session: only the exact wording/shape of the winner text and the concrete Acceptance Criteria bullets -- the substantive design itself is not being re-decided.
**When** implementing Composite design (4 required pieces, carried forward from session 7d98aa59 with the pre-mortem mitigation now folded in from the start): (a) Flat-kwargs threading -- add the 7 GW-02 kwargs (frozen_context, current_config, repo, ratchet_ref_name, callable_name, concrete_inputs, plus commit_tree_sha as now-optional) as new keyword params on both run_multi_iteration_loop and run_campaign_loop, forwarded unchanged layer to layer, matching the existing flat-kwarg convention. (b) Fitness carry-forward -- add fitness_dict: dict[str, float] as a new additive field on OneCandidatePassResult (populated alongside ratchet_decision); run_multi_iteration_loop keeps loop-local best_fitness/best_higher_is_better state, None for iteration 1 (triggers GW-02's existing automatic-accept sentinel), updated to result.fitness_dict ONLY when result.accepted is True, left unchanged on reject. (c) commit_tree_sha computation -- relax run_one_candidate_pass's commit_tree_sha to str | None = None (genuinely additive: existing callers passing a literal string are unaffected) and add a new commit_tree_sha_fn: Callable[[CandidateHandoff, Path, Path, str | None], str] = compute_candidate_tree_sha injection seam (same DI convention as guarded_evaluate_fn), invoked inside run_one_candidate_pass right after step 1 (handoff = dispatch_backend.dispatch_candidate()) when commit_tree_sha is None. The real default computes a whole-repo-tree substitution (base = current best-so-far tree via read_best_so_far, one path's blob replaced with the candidate's content) using a scratch GIT_INDEX_FILE (git read-tree + git update-index --add --cacheinfo + git write-tree), never a synthetic single-file tree -- required because reset_worktree_to_best_so_far does a real git reset --hard onto this lineage. (d) candidate_target_path: Path -- a new REQUIRED (no default) keyword parameter on run_one_candidate_pass, threaded flat from run_campaign_loop -> run_multi_iteration_loop -> run_one_candidate_pass -> commit_tree_sha_fn. The repo-relative destination path for the candidate's content inside the tree substitution; must be explicit and caller-supplied, never inferred from handoff.path's basename, because create_pending_commit/advance_best_so_far never validate tree semantics -- a guessed path would silently corrupt best-so-far git lineage with no exception and no failing test, exactly the risk this required parameter closes (omitting it raises TypeError immediately, fail-fast instead of silent corruption). UNLIKE commit_tree_sha's relaxation, this is NOT backward-compatible for existing direct-call test sites in PR #90's suite that construct a full run_one_candidate_pass call -- accepted as a named, scoped mechanical migration cost (updating those call sites), not a design flaw; a default-with-fallback was explicitly rejected because it would reintroduce the exact silent-inference failure mode this parameter exists to close. Explicitly documented scope boundary: does not support process-level crash-resume of loop-local fitness state (no existing resume entry point exists to attach recovery to; deferred to a future item).
**Then**
  - [ ] `OneCandidatePassResult.fitness_dict` equals exactly the dict returned by `guarded_evaluate_fn` for that call (byte-identical), verified by patching `guarded_evaluate_fn` with a fixture return value and asserting it round-trips onto the returned `OneCandidatePassResult`.
  - [ ] After an ACCEPTED candidate (`result.accepted is True`), the next iteration's `run_one_candidate_pass` call receives `best_fitness == <that candidate's fitness_dict>` (spy assertion on `run_multi_iteration_loop`'s consecutive calls).
  - [ ] After a REJECTED candidate (`result.accepted is False`), the next iteration's `run_one_candidate_pass` call receives `best_fitness` IDENTICAL to what was passed into the rejected call (not the rejected candidate's own fitness_dict) -- spy assertion across consecutive calls.
  - [ ] `compute_candidate_tree_sha`'s real default, given a fixture repo with a known best-so-far tree and a candidate file, produces a tree SHA whose `git ls-tree -r` output contains every base-tree path unchanged EXCEPT `candidate_target_path` (which contains the candidate's new content) -- explicitly asserted to NOT equal a tree containing only the single candidate file.
  - [ ] Calling `run_one_candidate_pass` without `candidate_target_path` raises `TypeError` before any dispatch/scoring/git call (Python's own keyword-only-required-param enforcement, verified via a direct call-signature test).
  - [ ] `commit_tree_sha` remains fully optional and backward-compatible: a test supplying a literal `commit_tree_sha` string continues to pass unchanged, with `commit_tree_sha_fn` never invoked (spy assertion it was not called).
  - [ ] Integration test: `run_campaign_loop(..., frozen_context=X, current_config=Y, repo=Z, ratchet_ref_name=W, callable_name=V, concrete_inputs=U, candidate_target_path=T)` forwards all 8 values unchanged through `run_multi_iteration_loop` into each `run_one_candidate_pass` call across at least 2 iterations (spy assertion on call kwargs each iteration).
  - [ ] Pre-implementation verification step (ALREADY DISCHARGED as of this spec, see the Pre-mortem Record's AI response above -- re-run immediately before shipping, not just once now): a repo-wide grep for existing `run_one_candidate_pass(` call sites outside the scoped `controller/` test selection, confirming the true blast radius before `candidate_target_path` ships. As of 2026-08-12: exactly 1 production call site (`controller/multi_iteration_loop.py:390`) + 28 test call sites (`tests/controller/test_main_loop.py`), no hidden callers found.
