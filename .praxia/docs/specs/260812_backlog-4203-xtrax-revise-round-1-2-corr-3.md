---
session_id: 48e73bf2
topic: Backlog #4203 (xtrax) REVISE round 1/2: correct the prior finalized spec (.praxia/docs/specs/260812_backlog-4203-xtrax-corrected-re-run-of-s.md, session c21cc4e2) after adversarial review found 5 blocking + 4 non-blocking objections (C1-C9) against threading GW-02's kwargs + candidate_target_path + commit_tree_sha_fn design through run_multi_iteration_loop/run_campaign_loop. Carry forward what survived review; fix only what adversarial review found broken.
task_type: architectural
winner: Composite corrective design resolving all 9 objections in-scope for this item, carrying forward the prior session's locked design (flat-kwargs threading convention, loop-local best_fitness carry-forward, commit_tree_sha_fn seam placement inside run_one_candidate_pass, candidate_target_path as a real required-when-needed parameter) and fixing exactly what adversarial review found broken:

C1 FIX -- higher_is_better origin: add `higher_is_better: Mapping[str, bool]` as a new REQUIRED (no default) keyword parameter on BOTH `run_multi_iteration_loop` and `run_campaign_loop`, threaded flat and forwarded UNCHANGED on every iteration -- joining the "static per-campaign, forwarded unchanged" bucket (frozen_context, current_config, repo, ratchet_ref_name, callable_name, concrete_inputs, commit_tree_sha), explicitly NOT the loop-local-mutated bucket (best_fitness). The "7 GW-02 kwargs" enumeration becomes 8; combined with candidate_target_path this item threads 9 new values total, not 8.

C2 FIX -- red-test-file scope: IN-SCOPE for this item, not deferred. Covers both (a) `tests/controller/test_multi_iteration_loop.py`'s `_base_kwargs()` helper and every `run_multi_iteration_loop(...)` call site, and `tests/controller/test_loop_run.py`'s `_base_kwargs()` helper and all 15 `run_campaign_loop(...)` call sites, updated to supply the 9 threaded values; AND (b) the two production forwarding call sites this fix necessarily touches anyway -- `run_multi_iteration_loop`'s own call to `run_one_candidate_pass` (currently `multi_iteration_loop.py:390-408`, forwards none of the 9) and `run_campaign_loop`'s own call to `run_multi_iteration_loop` (currently `loop_run.py:507-532`, same gap) -- both updated to forward all 9 values unchanged. AC gates on both test files passing AND an integration test spying on forwarded kwargs at each layer boundary.

C3 FIX -- bootstrap fallback: add `bootstrap_base_tree_sha: str | None = None` as a new optional kwarg on `run_one_candidate_pass`. Inside the function, resolve the tree-substitution base via `read_best_so_far(repo, ratchet_ref_name)` BEFORE invoking `commit_tree_sha_fn` (a second, side-effect-free read alongside the existing one used for `commit_parent_sha` resolution -- mirrors `resolve_derived_from`'s own precedent of being called twice deliberately). If that returns `None`, fall back to `bootstrap_base_tree_sha`; if that is ALSO `None`, `compute_candidate_tree_sha` (the real default) raises `ValueError` immediately -- exactly mirroring `commit_parent_sha`'s existing "ValueError if both are None on an accept" precedent, never a silent guess (rejects the HEAD^{tree}-fallback alternative for the same reason candidate_target_path's default-with-fallback was rejected in the prior session).

C4 FIX -- commit_tree_sha_fn signature: `Callable[[CandidateHandoff, Path, Path, str | None], str]` becomes named params `(handoff: CandidateHandoff, repo: Path, candidate_target_path: Path, base_sha: str | None) -> str`. `handoff` supplies the candidate's real on-disk content (via `handoff.path`) to hash-object into the new tree. `base_sha` is the resolved base COMMIT to substitute against (read_best_so_far's result, or the C3 bootstrap fallback) -- explicitly and unambiguously NOT `commit_tree_sha` itself, which is this function's own return value, never one of its inputs.

C5 FIX (cheap) -- git plumbing recipe, 3 previously-missing steps named explicitly: (1) `base_sha` is a COMMIT sha; resolve `base_sha^{tree}` (e.g. via `git rev-parse`) before `git read-tree` into the scratch `GIT_INDEX_FILE`. (2) Write the candidate's blob FIRST via `git hash-object -w <handoff.path>` to obtain a blob sha, BEFORE `git update-index --add --cacheinfo`. (3) File mode is `100644` (regular, non-executable) unconditionally -- staging an executable candidate is an explicitly out-of-scope future extension, not silently guessed or auto-detected from the source file's own permission bits.

C6 FIX -- crash-resume lineage conflict: new module-local exception `PriorBestSoFarLineageConflictError` in `main_loop.py`, raised inside the `best_fitness is None` branch (BEFORE constructing the automatic-accept sentinel) when `read_best_so_far(repo, ratchet_ref_name)` returns non-`None` -- naming both `ratchet_ref_name` and the conflicting SHA in the message. New kwarg `allow_fresh_start_despite_existing_lineage: bool = False`, threaded flat through `run_multi_iteration_loop`/`run_campaign_loop` alongside the other static kwargs -- when `True`, the caller has explicitly acknowledged the conflict and today's automatic-accept-sentinel behavior proceeds unchanged (silent overwrite becomes opt-in, never a silent default). Full crash-resume (re-deriving loop-local best_fitness FROM the existing git lineage) remains explicitly out of scope, exactly as the prior spec already established -- this fix only closes the SILENT half of the gap.

C7 FIX (cheap) -- `candidate_target_path: Path | None = None` (was: unconditionally required). New validation, evaluated immediately (before dispatch/any git call): if `commit_tree_sha is None` AND `candidate_target_path is None`, raise `ValueError` ("candidate_target_path is required when commit_tree_sha is not supplied directly"). Literal-`commit_tree_sha` call sites (e.g. `test_main_loop.py`'s `_new_step_kwargs()` with `'unused-tree-sha'`) may omit it entirely.

C8 FIX (cheap) -- AC bullet 1 (fitness_dict round-trip) gets one added clause explicitly restating the no-partial-construction-on-exception precedent already established for `ratchet_decision`: an exception anywhere upstream of `guarded_evaluate_fn`'s return aborts the whole `run_one_candidate_pass` call before any `OneCandidatePassResult` -- partial or otherwise -- is constructed, so `fitness_dict` is never observed in a half-populated state.

C9 FIX (cheap) -- explicit note that `git update-index --add --cacheinfo` does not distinguish "new path" from "replace existing path" at the plumbing level (both are just staging an index entry), so `compute_candidate_tree_sha` needs no special-casing; new AC bullet requires a test where `candidate_target_path` is ABSENT from the base tree (not just the replacement case), asserting the new path appears correctly in the resulting tree via `git ls-tree -r`.
created_at: 2026-08-12T15:19:56.095448+00:00
---

# Brainstorm: Backlog #4203 (xtrax) REVISE round 1/2: correct the prior finalized spec (.praxia/docs/specs/260812_backlog-4203-xtrax-corrected-re-run-of-s.md, session c21cc4e2) after adversarial review found 5 blocking + 4 non-blocking objections (C1-C9) against threading GW-02's kwargs + candidate_target_path + commit_tree_sha_fn design through run_multi_iteration_loop/run_campaign_loop. Carry forward what survived review; fix only what adversarial review found broken.

## Problem Frame
Fixed constraints (carried forward from c21cc4e2 unchanged, not re-open for debate): (1) run_one_candidate_pass's existing signature stays additive-only for all pre-existing callers passing literal commit_tree_sha strings -- no breaking changes there. (2) compute_ratchet_decision's signature is locked (higher_is_better is its required kwarg -- confirmed at multi_metric_ratchet.py:152). (3) create_pending_commit requires a real git TREE object sha (confirmed ratchet_crash_atomicity.py:91). (4) The file's existing Callable[..., X]=real_default injection-seam convention governs any new computed-value seam (matches guarded_evaluate_fn, stats_battery_fn, etc.). (5) run_multi_iteration_loop's per-candidate loop structure stays -- one call to run_one_candidate_pass per iteration, no restructuring. (6) Zero retry logic anywhere in this scope. (7) commit_tree_sha_fn seam must live INSIDE run_one_candidate_pass, not one level up (handoff.path doesn't exist until dispatch runs inside that function -- locked finding from 7d98aa59). (8) best_fitness carry-forward is loop-local Python state (not git-notes), updated only on accept -- locked finding from 7d98aa59.

Newly fixed by THIS session's own direct verification (not re-open): higher_is_better and best_fitness must be supplied together or both omitted (ValueError otherwise, main_loop.py:461-467) -- so whatever mechanism supplies best_fitness to run_one_candidate_pass must supply higher_is_better through the same call, every time.

Negotiable in this session: (a) where higher_is_better originates and how it threads (loop-local vs caller-supplied-once) -- C1. (b) whether fixing the two currently-red test files is this item's scope or a named other item's -- C2. (c) the bootstrap mechanism for commit_tree_sha_fn's first-candidate case -- C3. (d) concrete parameter names for commit_tree_sha_fn -- C4. (e) the crash-resume-lineage-conflict detection/acknowledgment mechanism -- C6. (f) the 4 non-blocking fixes' exact shape.

Confirmed frame -- ready for Phase 2 divergence.

## Idea Pool
- [user] Ideation round 1 -- C1 (higher_is_better origin) and C2 (red-test-file scope), at least two real alternatives each:
- [user] C1-Alt-A (primary candidate): higher_is_better as a new REQUIRED kwarg (Mapping[str, bool], no default) on both run_multi_iteration_loop and run_campaign_loop, threaded flat and forwarded UNCHANGED on every iteration -- same bucket as frozen_context/current_config/repo/ratchet_ref_name/callable_name/concrete_inputs/commit_tree_sha (the "static per-campaign, forwarded unchanged" bucket), NOT the same bucket as best_fitness (loop-local, mutated on accept). Rationale: metric direction (accuracy=higher-is-better, loss=lower-is-better) is a property of the fitness_dict's schema, fixed for the whole campaign, not something that changes candidate-to-candidate the way best_fitness does.
- [user] C1-Alt-B: derive higher_is_better implicitly from frozen_context or some other already-threaded value (e.g. infer from BathosFrozenContext) instead of adding a 9th top-level kwarg -- avoids growing the kwarg count further.
- [user] C1-Alt-C: make higher_is_better loop-local state alongside best_fitness (i.e. caller supplies it only once via an initial best_higher_is_better seed, and it free-rides in the same mutable-state slot) rather than a separate always-forwarded parameter.
- [user] C2-Alt-A (in-scope): add explicit AC criteria requiring test_multi_iteration_loop.py and test_loop_run.py to pass after implementation -- since #4203 is literally the reason these files need the new kwargs threaded, leaving them red after this ships would mean the item didn't actually accomplish its own stated purpose.
- [user] C2-Alt-B (defer): treat fixing the two test files' _base_kwargs() helpers and all their call sites as a separate, explicitly-named follow-up item, since the prior spec's own file-set boundary was controller/main_loop.py + the new compute_candidate_tree_sha default, not a full test-suite migration.
- [user] Ideation round 2 -- C3 (bootstrap fallback), C4 (commit_tree_sha_fn signature naming), C5 (git plumbing recipe gaps):
- [user] C3-Alt-A (primary): mirror commit_parent_sha's existing pattern exactly. Add a new optional kwarg `bootstrap_base_tree_sha: str | None = None` on run_one_candidate_pass. Inside run_one_candidate_pass, resolve the base once via read_best_so_far(repo, ratchet_ref_name) BEFORE invoking commit_tree_sha_fn (duplicating that read is fine -- resolve_derived_from is already called twice in this file for the same reason, side-effect-free reads). If read_best_so_far returns None, fall back to bootstrap_base_tree_sha; if THAT is also None, the real default compute_candidate_tree_sha raises ValueError immediately (fail loud), exactly mirroring commit_parent_sha's own "ValueError if both are None on an accept" precedent.
- [user] C3-Alt-B: instead of a caller-supplied bootstrap fallback, have compute_candidate_tree_sha's default fall back to the REPO'S OWN CURRENT HEAD tree (git rev-parse HEAD^{tree}) when no prior best-so-far exists -- reasoning: the very first candidate's "base" is legitimately "whatever the repo looked like before any ratcheting started," which HEAD already represents, no new parameter needed. Rejected on reflection: silently assumes HEAD is meaningful/checked-out correctly at call time, and reintroduces exactly the "guessed instead of explicit" failure shape C1's candidate_target_path fix was designed to avoid -- an explicit required-or-raise parameter is more consistent with this file's own established convention (commit_parent_sha) than an implicit git-state guess.
- [user] C4 (signature naming, converged, low controversy): name the 4 params (handoff: CandidateHandoff, repo: Path, candidate_target_path: Path, base_sha: str | None) -- base_sha is the resolved base commit to substitute against (read_best_so_far's result or the bootstrap fallback from C3-Alt-A), explicitly NOT commit_tree_sha (that's the function's own return value, never an input to itself).
- [user] C5 (cheap, converged): the winner text's git-plumbing recipe explicitly states 3 steps currently missing: (1) base_sha is a COMMIT sha -- resolve `base_sha^{tree}` before `git read-tree`; (2) write the candidate's blob first via `git hash-object -w <handoff.path>` to get a blob sha BEFORE `git update-index --add --cacheinfo`; (3) file mode is 100644 (regular, non-executable) unconditionally -- staging an executable candidate is an explicitly out-of-scope future extension, not silently guessed.
- [user] Ideation round 3 -- C6 (crash-resume lineage conflict), C7 (conditional candidate_target_path), C8 (AC bullet 1 precedent restatement), C9 (new-path-not-replacement scenario):
- [user] C6-Alt-A (primary, fail-loud default with explicit override): when run_one_candidate_pass is invoked with best_fitness=None (the fresh-loop/first-candidate branch) AND read_best_so_far(repo, ratchet_ref_name) returns non-None (a prior best-so-far commit already exists in git lineage), this is the exact restart-vs-fresh-state conflict C6 describes. Add a new exception PriorBestSoFarLineageConflictError (module-local to main_loop.py) raised in that exact condition, naming the ref and the conflicting SHA in the message. Add a new kwarg `allow_fresh_start_despite_existing_lineage: bool = False` -- when True, the caller has explicitly acknowledged the conflict and the existing automatic-accept-sentinel behavior proceeds unchanged (silently overwriting is now an opt-in, not a silent default). Threaded flat through run_multi_iteration_loop/run_campaign_loop like the other static kwargs.
- [user] C6-Alt-B: instead of a boolean override, require the caller to pass the EXPECTED prior-best sha explicitly (expected_prior_best_sha: str | None) and compare it against read_best_so_far's actual result -- raises if they mismatch, passes silently if they match (including both None). More precise (catches a wrong-sha as well as an unexpected-any-sha), but heavier: forces every fresh-campaign caller to explicitly pass None even in the common case, and doesn't obviously improve over the simpler boolean for what C6 actually asked for (detect + fail-loud OR explicit acknowledgment). Rejected in favor of Alt-A for simplicity matching the file's existing convention (booleans like no_sidecar, not comparison sentinels) -- flagged as a documented alternative, not silently dropped.
- [user] C7 (cheap, converged): candidate_target_path: Path | None = None (was: unconditionally required). New validation: if commit_tree_sha is None AND candidate_target_path is None, raise ValueError immediately (before dispatch/any git call) -- "candidate_target_path is required when commit_tree_sha is not supplied directly." Preserves fail-fast for the real-usage case while letting literal-commit_tree_sha callers (test_main_loop.py's _new_step_kwargs with 'unused-tree-sha') omit it entirely, matching C7's own observation.
- [user] C8 (cheap, converged): AC bullet 1 gets one added sentence: "matching the no-partial-construction-on-exception precedent already established for ratchet_decision (OneCandidatePassResult's own docstring) -- an exception anywhere upstream of guarded_evaluate_fn's return aborts the whole call before any OneCandidatePassResult, partial or otherwise, is constructed, so fitness_dict is never observed in a half-populated state."
- [user] C9 (cheap, converged): explicitly note in the winner text that `git update-index --add --cacheinfo` does not distinguish "new path" from "replace existing path" at the plumbing level -- both are just staging an index entry for that path, so no special-casing is needed in compute_candidate_tree_sha itself -- but add an explicit AC bullet requiring a test where candidate_target_path is ABSENT from the base tree (not just the replacement case), asserting the new path appears correctly in the resulting tree.
- [user] Divergence exhausted -- all 9 objections have at least one concrete resolution, with real alternatives considered and rejected on their merits for C3 and C6 (the two genuinely open design points), and C1/C2/C4/C5/C7/C8/C9 converged with low controversy. Ready to converge.

## Decision Log
- [REJECT] C1-Alt-B: derive higher_is_better implicitly from frozen_context/BathosFrozenContext instead of a new kwarg: BathosFrozenContext (controller/evaluate_adapter.py) carries a locked ClosureManifest + score_fn + campaign_id -- nothing in its shape encodes per-metric optimization direction. Inventing that encoding would mean silently overloading an already-locked, already-merged dataclass's meaning outside this item's file scope (frozen_context's shape is owned by GW-02/evaluate_adapter, not this item) -- the same "don't guess, require explicit" reasoning that killed candidate_target_path's default-with-fallback option in the prior session.
- [REJECT] C1-Alt-C: fold higher_is_better into the same loop-local mutable-state slot as best_fitness: higher_is_better is fixed metric-direction schema for the whole campaign (accuracy is always higher-is-better), not per-candidate state that changes on accept the way best_fitness does. Coupling it to best_fitness's mutable lifecycle would make it silently reset alongside best_fitness on a future crash-resume feature, which is exactly backwards -- direction shouldn't be re-derived per restart, only the numeric best should.
- [REJECT] C2-Alt-B: defer test_multi_iteration_loop.py/test_loop_run.py fixes to a separate named item: These two files exist specifically to test the functions #4203 is modifying: leaving them red after this item ships means the item did not accomplish its own stated purpose (thread GW-02 kwargs through the multi-iteration/campaign layers) in any verifiable way. Deferring would also leave a second adversarial-review round rediscovering exactly this gap. Accepted as in-scope, with the added precision from the forcing-beat risk check: covers both the two test-file helpers AND the two production forwarding call sites end-to-end, not just the fixtures.
- [REJECT] C3-Alt-B: compute_candidate_tree_sha default falls back to repo's current HEAD^{tree} when no prior best-so-far exists, instead of a caller-supplied bootstrap parameter: Silently assumes HEAD is meaningful/checked-out correctly at call time -- reintroduces the exact "guessed instead of explicit" failure shape that candidate_target_path's required-parameter fix (prior session) was designed to close. An explicit bootstrap_base_tree_sha parameter with a hard ValueError if omitted is more consistent with this file's own established convention (commit_parent_sha's identical bootstrap pattern) than an implicit git-state guess.
- [REJECT] C6-Alt-B: expected_prior_best_sha comparison parameter instead of a boolean acknowledgment flag: More precise (also catches a wrong-sha, not just an unexpected-any-sha) but heavier: forces every fresh-campaign caller to explicitly pass None even in the common case, and doesn't clearly improve on what C6 actually asked for (detect + fail-loud OR explicit acknowledgment). The simpler boolean (allow_fresh_start_despite_existing_lineage) matches this file's existing convention for opt-in flags (no_sidecar, agent_mode) rather than introducing a new comparison-sentinel pattern.
- [ACCEPT] Acceptance Criteria (Then) -- concrete measurable bullets covering all 9 objections, recorded here because this contemplex session's Then/AI-response slots were not wired to reply content post-finalize (same tool limitation as prior session c21cc4e2) -- persisted verbatim as a decision record instead: [C1] Calling run_multi_iteration_loop or run_campaign_loop without higher_is_better raises TypeError (no default) before any candidate is dispatched; a spy on run_one_candidate_pass's consecutive calls shows higher_is_better forwarded byte-identical on every iteration. [C2] After implementation: (a) tests/controller/test_multi_iteration_loop.py's full suite passes, with _base_kwargs() supplying all 9 threaded values; (b) tests/controller/test_loop_run.py's full suite passes, with its _base_kwargs() likewise updated across all 15 call sites; (c) an integration test spies on run_multi_iteration_loop's own call to run_one_candidate_pass (multi_iteration_loop.py:390-408) AND run_campaign_loop's own call to run_multi_iteration_loop (loop_run.py:507-532), asserting all 9 values (frozen_context, current_config, repo, ratchet_ref_name, commit_tree_sha, callable_name, concrete_inputs, higher_is_better, candidate_target_path) are forwarded unchanged at both layer boundaries across >=2 iterations. [C3] Given a fixture repo with no ratchet_ref_name ref yet (read_best_so_far returns None): (a) supplying bootstrap_base_tree_sha lets compute_candidate_tree_sha's default proceed and produce a valid tree; (b) omitting it raises ValueError before any git write, mirroring commit_parent_sha's existing bootstrap-required error. [C4] A direct signature-inspection test (inspect.signature) asserts compute_candidate_tree_sha's real default has exactly the 4 named parameters (handoff, repo, candidate_target_path, base_sha) in that order. [C5] compute_candidate_tree_sha's default, given a fixture repo with a known base commit and a candidate file: resolves base_sha^{tree} before git read-tree (assert via a spy/real git call sequence), writes the candidate blob via git hash-object -w before git update-index --add --cacheinfo, and the resulting tree's git ls-tree -r entry for candidate_target_path shows mode 100644. [C6] Given best_fitness=None and read_best_so_far(repo, ratchet_ref_name) returning a real prior SHA: (a) the default (allow_fresh_start_despite_existing_lineage=False) raises PriorBestSoFarLineageConflictError naming both ratchet_ref_name and the conflicting SHA, before any dispatch/bathos/git call; (b) allow_fresh_start_despite_existing_lineage=True proceeds with the existing automatic-accept-sentinel behavior unchanged. [C7] Supplying a literal commit_tree_sha with candidate_target_path omitted succeeds with commit_tree_sha_fn never invoked (spy assertion); supplying neither raises ValueError immediately. [C8] fitness_dict round-trip test explicitly asserts no partial OneCandidatePassResult is ever observable when guarded_evaluate_fn raises (patched to raise mid-call), mirroring the existing ratchet_decision precedent test. [C9] A test with candidate_target_path ABSENT from the base tree (a genuinely new path) asserts the resulting tree contains that new path with the candidate's content, alongside every pre-existing base-tree path unchanged.
- [ACCEPT] Pre-mortem AI response (recorded here for the same reason as the AC decision above -- the session's dedicated AI pre-mortem slot was not wired to reply content post-finalize): Valid failure mode, and it sharpens rather than invalidates the C6 fix -- mitigation folds into the AC and the winner text's own framing, not a redesign: (1) the winner text is explicit this closes only the SILENT half of the gap (detecting an unacknowledged conflict), not a "whose lineage is it" classification -- that would require actual crash-resume (comparing loop-local identity/task_id against the ref's commit trailer), which both this spec and its predecessor already scope OUT as a distinct, not-yet-designed capability requiring its own resume entry point. (2) The premortem's own "engineer sets the flag to True everywhere in a wrapper" failure mode is a MISUSE of an explicit, named, documented escape hatch, not a design flaw in the primitive itself -- exactly analogous to how a caller COULD pass no_sidecar=True everywhere too; the fix's job is to make the silent case loud, not to make misuse of an intentionally-provided override impossible. (3) Concrete mitigation added to the AC: the PriorBestSoFarLineageConflictError message (already specified to name ratchet_ref_name and the conflicting SHA) must ALSO instruct the caller to use a fresh, unique ratchet_ref_name per genuinely-new campaign rather than reaching for the override flag, when they don't intend to resume prior lineage -- steering the tired engineer toward the actually-correct fix (a new ref name) instead of the footgun (blanket True). This requires only specifying the exact wording of the raised error, which the AC gates on.

## Assumptions

## TBDs

## Pre-mortem Record
**User:** **User:** Most likely failure mode 6 months out: the `allow_fresh_start_despite_existing_lineage` flag (C6 fix) becomes a footgun rather than a safeguard -- because it defaults to `False` and raises `PriorBestSoFarLineageConflictError` on ANY fresh-loop-state-meets-existing-lineage condition, a caller who legitimately wants to start a brand-new, unrelated campaign against a repo that happens to already have some OTHER old `ratchet_ref_name` value lying around from a past experiment (not a crash-restart at all, just ref-name reuse or a stale ref nobody cleaned up) hits this exception too, and the "obvious" fix a tired engineer reaches for is to just set the flag to `True` everywhere by default in their own wrapper -- silently defeating the whole protection C6 was built for, because the flag doesn't distinguish "this really is a restart with stale-but-real lineage" from "this ref name coincidentally already has unrelated old data in it." The fix never asked WHOSE lineage this is, only WHETHER lineage exists.
**AI:** Valid failure mode, and it sharpens rather than invalidates the C6 fix -- mitigation folds into the AC and the winner text's own framing, not a redesign: (1) the winner text is explicit this closes only the SILENT half of the gap (detecting an unacknowledged conflict), not a "whose lineage is it" classification -- that would require actual crash-resume (comparing loop-local identity/task_id against the ref's commit trailer), which both this spec and its predecessor already scope OUT as a distinct, not-yet-designed capability requiring its own resume entry point. (2) The premortem's own "engineer sets the flag to True everywhere in a wrapper" failure mode is a MISUSE of an explicit, named, documented escape hatch, not a design flaw in the primitive itself -- exactly analogous to how a caller COULD pass no_sidecar=True everywhere too; the fix's job is to make the silent case loud, not to make misuse of an intentionally-provided override impossible. (3) Concrete mitigation added to the AC: the PriorBestSoFarLineageConflictError message (already specified to name ratchet_ref_name and the conflicting SHA) must ALSO instruct the caller to use a fresh, unique ratchet_ref_name per genuinely-new campaign rather than reaching for the override flag, when they don't intend to resume prior lineage -- steering the tired engineer toward the actually-correct fix (a new ref name) instead of the footgun (blanket True). This requires only specifying the exact wording of the raised error, which the AC (bullet C6) gates on.

## Acceptance Criteria
**Given** Fixed constraints (carried forward from c21cc4e2 unchanged, not re-open for debate): (1) run_one_candidate_pass's existing signature stays additive-only for all pre-existing callers passing literal commit_tree_sha strings -- no breaking changes there. (2) compute_ratchet_decision's signature is locked (higher_is_better is its required kwarg -- confirmed at multi_metric_ratchet.py:152). (3) create_pending_commit requires a real git TREE object sha (confirmed ratchet_crash_atomicity.py:91). (4) The file's existing Callable[..., X]=real_default injection-seam convention governs any new computed-value seam (matches guarded_evaluate_fn, stats_battery_fn, etc.). (5) run_multi_iteration_loop's per-candidate loop structure stays -- one call to run_one_candidate_pass per iteration, no restructuring. (6) Zero retry logic anywhere in this scope. (7) commit_tree_sha_fn seam must live INSIDE run_one_candidate_pass, not one level up (handoff.path doesn't exist until dispatch runs inside that function -- locked finding from 7d98aa59). (8) best_fitness carry-forward is loop-local Python state (not git-notes), updated only on accept -- locked finding from 7d98aa59.

Newly fixed by THIS session's own direct verification (not re-open): higher_is_better and best_fitness must be supplied together or both omitted (ValueError otherwise, main_loop.py:461-467) -- so whatever mechanism supplies best_fitness to run_one_candidate_pass must supply higher_is_better through the same call, every time.

Negotiable in this session: (a) where higher_is_better originates and how it threads (loop-local vs caller-supplied-once) -- C1. (b) whether fixing the two currently-red test files is this item's scope or a named other item's -- C2. (c) the bootstrap mechanism for commit_tree_sha_fn's first-candidate case -- C3. (d) concrete parameter names for commit_tree_sha_fn -- C4. (e) the crash-resume-lineage-conflict detection/acknowledgment mechanism -- C6. (f) the 4 non-blocking fixes' exact shape.

Confirmed frame -- ready for Phase 2 divergence.
**When** implementing Composite corrective design resolving all 9 objections in-scope for this item, carrying forward the prior session's locked design (flat-kwargs threading convention, loop-local best_fitness carry-forward, commit_tree_sha_fn seam placement inside run_one_candidate_pass, candidate_target_path as a real required-when-needed parameter) and fixing exactly what adversarial review found broken:

C1 FIX -- higher_is_better origin: add `higher_is_better: Mapping[str, bool]` as a new REQUIRED (no default) keyword parameter on BOTH `run_multi_iteration_loop` and `run_campaign_loop`, threaded flat and forwarded UNCHANGED on every iteration -- joining the "static per-campaign, forwarded unchanged" bucket (frozen_context, current_config, repo, ratchet_ref_name, callable_name, concrete_inputs, commit_tree_sha), explicitly NOT the loop-local-mutated bucket (best_fitness). The "7 GW-02 kwargs" enumeration becomes 8; combined with candidate_target_path this item threads 9 new values total, not 8.

C2 FIX -- red-test-file scope: IN-SCOPE for this item, not deferred. Covers both (a) `tests/controller/test_multi_iteration_loop.py`'s `_base_kwargs()` helper and every `run_multi_iteration_loop(...)` call site, and `tests/controller/test_loop_run.py`'s `_base_kwargs()` helper and all 15 `run_campaign_loop(...)` call sites, updated to supply the 9 threaded values; AND (b) the two production forwarding call sites this fix necessarily touches anyway -- `run_multi_iteration_loop`'s own call to `run_one_candidate_pass` (currently `multi_iteration_loop.py:390-408`, forwards none of the 9) and `run_campaign_loop`'s own call to `run_multi_iteration_loop` (currently `loop_run.py:507-532`, same gap) -- both updated to forward all 9 values unchanged. AC gates on both test files passing AND an integration test spying on forwarded kwargs at each layer boundary.

C3 FIX -- bootstrap fallback: add `bootstrap_base_tree_sha: str | None = None` as a new optional kwarg on `run_one_candidate_pass`. Inside the function, resolve the tree-substitution base via `read_best_so_far(repo, ratchet_ref_name)` BEFORE invoking `commit_tree_sha_fn` (a second, side-effect-free read alongside the existing one used for `commit_parent_sha` resolution -- mirrors `resolve_derived_from`'s own precedent of being called twice deliberately). If that returns `None`, fall back to `bootstrap_base_tree_sha`; if that is ALSO `None`, `compute_candidate_tree_sha` (the real default) raises `ValueError` immediately -- exactly mirroring `commit_parent_sha`'s existing "ValueError if both are None on an accept" precedent, never a silent guess (rejects the HEAD^{tree}-fallback alternative for the same reason candidate_target_path's default-with-fallback was rejected in the prior session).

C4 FIX -- commit_tree_sha_fn signature: `Callable[[CandidateHandoff, Path, Path, str | None], str]` becomes named params `(handoff: CandidateHandoff, repo: Path, candidate_target_path: Path, base_sha: str | None) -> str`. `handoff` supplies the candidate's real on-disk content (via `handoff.path`) to hash-object into the new tree. `base_sha` is the resolved base COMMIT to substitute against (read_best_so_far's result, or the C3 bootstrap fallback) -- explicitly and unambiguously NOT `commit_tree_sha` itself, which is this function's own return value, never one of its inputs.

C5 FIX (cheap) -- git plumbing recipe, 3 previously-missing steps named explicitly: (1) `base_sha` is a COMMIT sha; resolve `base_sha^{tree}` (e.g. via `git rev-parse`) before `git read-tree` into the scratch `GIT_INDEX_FILE`. (2) Write the candidate's blob FIRST via `git hash-object -w <handoff.path>` to obtain a blob sha, BEFORE `git update-index --add --cacheinfo`. (3) File mode is `100644` (regular, non-executable) unconditionally -- staging an executable candidate is an explicitly out-of-scope future extension, not silently guessed or auto-detected from the source file's own permission bits.

C6 FIX -- crash-resume lineage conflict: new module-local exception `PriorBestSoFarLineageConflictError` in `main_loop.py`, raised inside the `best_fitness is None` branch (BEFORE constructing the automatic-accept sentinel) when `read_best_so_far(repo, ratchet_ref_name)` returns non-`None` -- naming both `ratchet_ref_name` and the conflicting SHA in the message. New kwarg `allow_fresh_start_despite_existing_lineage: bool = False`, threaded flat through `run_multi_iteration_loop`/`run_campaign_loop` alongside the other static kwargs -- when `True`, the caller has explicitly acknowledged the conflict and today's automatic-accept-sentinel behavior proceeds unchanged (silent overwrite becomes opt-in, never a silent default). Full crash-resume (re-deriving loop-local best_fitness FROM the existing git lineage) remains explicitly out of scope, exactly as the prior spec already established -- this fix only closes the SILENT half of the gap.

C7 FIX (cheap) -- `candidate_target_path: Path | None = None` (was: unconditionally required). New validation, evaluated immediately (before dispatch/any git call): if `commit_tree_sha is None` AND `candidate_target_path is None`, raise `ValueError` ("candidate_target_path is required when commit_tree_sha is not supplied directly"). Literal-`commit_tree_sha` call sites (e.g. `test_main_loop.py`'s `_new_step_kwargs()` with `'unused-tree-sha'`) may omit it entirely.

C8 FIX (cheap) -- AC bullet 1 (fitness_dict round-trip) gets one added clause explicitly restating the no-partial-construction-on-exception precedent already established for `ratchet_decision`: an exception anywhere upstream of `guarded_evaluate_fn`'s return aborts the whole `run_one_candidate_pass` call before any `OneCandidatePassResult` -- partial or otherwise -- is constructed, so `fitness_dict` is never observed in a half-populated state.

C9 FIX (cheap) -- explicit note that `git update-index --add --cacheinfo` does not distinguish "new path" from "replace existing path" at the plumbing level (both are just staging an index entry), so `compute_candidate_tree_sha` needs no special-casing; new AC bullet requires a test where `candidate_target_path` is ABSENT from the base tree (not just the replacement case), asserting the new path appears correctly in the resulting tree via `git ls-tree -r`.
**Then**
  - [ ] [C1] Calling `run_multi_iteration_loop` or `run_campaign_loop` without `higher_is_better` raises `TypeError` (no default) before any candidate is dispatched; a spy on `run_one_candidate_pass`'s consecutive calls shows `higher_is_better` forwarded byte-identical on every iteration.
  - [ ] [C2] After implementation: (a) `tests/controller/test_multi_iteration_loop.py`'s full suite passes, with `_base_kwargs()` supplying all 9 threaded values; (b) `tests/controller/test_loop_run.py`'s full suite passes, with its `_base_kwargs()` likewise updated across all 15 call sites; (c) an integration test spies on `run_multi_iteration_loop`'s own call to `run_one_candidate_pass` (`multi_iteration_loop.py:390-408`) AND `run_campaign_loop`'s own call to `run_multi_iteration_loop` (`loop_run.py:507-532`), asserting all 9 values (`frozen_context`, `current_config`, `repo`, `ratchet_ref_name`, `commit_tree_sha`, `callable_name`, `concrete_inputs`, `higher_is_better`, `candidate_target_path`) are forwarded unchanged at both layer boundaries across >=2 iterations.
  - [ ] [C3] Given a fixture repo with no `ratchet_ref_name` ref yet (`read_best_so_far` returns `None`): (a) supplying `bootstrap_base_tree_sha` lets `compute_candidate_tree_sha`'s default proceed and produce a valid tree; (b) omitting it raises `ValueError` before any git write, mirroring `commit_parent_sha`'s existing bootstrap-required error.
  - [ ] [C4] A direct signature-inspection test (`inspect.signature`) asserts `compute_candidate_tree_sha`'s real default has exactly the 4 named parameters (`handoff`, `repo`, `candidate_target_path`, `base_sha`) in that order.
  - [ ] [C5] `compute_candidate_tree_sha`'s default, given a fixture repo with a known base commit and a candidate file: resolves `base_sha^{tree}` before `git read-tree` (assert via a spy/real git call sequence), writes the candidate blob via `git hash-object -w` before `git update-index --add --cacheinfo`, and the resulting tree's `git ls-tree -r` entry for `candidate_target_path` shows mode `100644`.
  - [ ] [C6] Given `best_fitness=None` and `read_best_so_far(repo, ratchet_ref_name)` returning a real prior SHA: (a) the default (`allow_fresh_start_despite_existing_lineage=False`) raises `PriorBestSoFarLineageConflictError` naming both `ratchet_ref_name` and the conflicting SHA, before any dispatch/bathos/git call, with the message instructing the caller to use a fresh, unique `ratchet_ref_name` per genuinely-new campaign rather than reaching for the override flag; (b) `allow_fresh_start_despite_existing_lineage=True` proceeds with the existing automatic-accept-sentinel behavior unchanged.
  - [ ] [C7] Supplying a literal `commit_tree_sha` with `candidate_target_path` omitted succeeds with `commit_tree_sha_fn` never invoked (spy assertion); supplying neither raises `ValueError` immediately.
  - [ ] [C8] `fitness_dict` round-trip test explicitly asserts no partial `OneCandidatePassResult` is ever observable when `guarded_evaluate_fn` raises (patched to raise mid-call), mirroring the existing `ratchet_decision` precedent test.
  - [ ] [C9] A test with `candidate_target_path` ABSENT from the base tree (a genuinely new path) asserts the resulting tree contains that new path with the candidate's content, alongside every pre-existing base-tree path unchanged.
