---
task_id: 260702_research-roadmap-dags
date: 2026-07-02
status: draft — pending adversarial spec review + user final review
sources:
  - .praxia/docs/research/260702_roadmap-research-synthesis.md (13-agent verified synthesis)
  - .praxia/docs/specs/260702_design-the-2181-agentic-algorithm-evolut.md (contemplex 129ecc73, INVEST PASS)
  - .praxia/docs/specs/260702_design-2174-next-slices-minimal-composit.md (contemplex 87f306f8, INVEST PASS)
  - .praxia/docs/specs/260702_xtrax-workflows-as-a-praxia-plugin-packa.md (contemplex 8ded6692, INVEST PASS, S-overridden)
  - .praxia/research/260702_nlm_prequery_roadmap.md (9 NLM queries over 5 designated notebooks)
---

# Roadmap: xtrax research epics — autoresearch, composition substrate, workflow plugin

## Where we stand (2026-07-02)

**Released baseline:** xtrax **0.3.0 live on PyPI** (OIDC trusted publishing, PyPI-only by
decision; TestPyPI dropped). Foundation epics all complete: #1573 audit framework, #1451
distribution readiness, #2175 agent composition tooling (capability registry 0.2.0), #2180 port
validation pipeline (scaffolded end-to-end, one example kernel). Open epics: **#2174**
(composition layer; E1 inference + E2 CLI shipped) and **#2181** (autoresearch; research
complete, spec'd by this cycle).

## Locked decisions (user, 2026-07-02)

1. **NS placement** — the neuro-symbolic notebook grounds BOTH epics; no standalone epic. One
   #2174 node: *typed composition IR + schema-constrained authoring* (after E1/E2). Its
   deterministic validation-gate artifact (extract_schema + validate_plan_topology + jaxlint →
   audit_verdict) is an **entry-criterion edge for #2181**.
2. **#2181 dependency narrowed** — from all-of-#2174 to the *minimal composition substrate*
   child item (mutation boundaries, sealed EvaluateFn seam, executed JIT-safe sidecar
   boundaries). Chain-map UI is NOT on #2181's critical path.
3. **PyPI-only publishing** — TestPyPI removed from publish.yml; gate records reconciled.
4. Delegated forks, resolved by brainstorm (consistently across sessions):
   **campaign-DAG ownership → bathos** (`campaign_edges`; xtrax stays truth-emitting);
   **loop dispatch substrate → pure-xtrax + bathos MCP** (Claude PCW; zero praxia coupling for
   the MVP; strict-mode/praxia dispatch is a phase-2 option gated on an MCP-reachability probe).

## The three threads and their DAG docs

| Thread | Spec (INVEST-passed) | DAG doc |
|---|---|---|
| **T1 — #2174 next slices** (substrate child + IR node) | `260702_design-2174-next-slices-minimal-composit.md` | `260702_01-dag-2174-substrate.md` |
| **T2 — #2181 autoresearch loop** (evaluator-first ratchet MVP) | `260702_design-the-2181-agentic-algorithm-evolut.md` | `260702_02-dag-2181-autoresearch.md` |
| **T3 — workflows-as-praxia-plugin** (phased dual-track) | `260702_xtrax-workflows-as-a-praxia-plugin-packa.md` | `260702_03-dag-plugin-workflows.md` |

Cross-repo build items land in **bathos** (9 items: `bathos[stats]` battery, `Run.seed`,
`campaign_edges`, sidecar drift detection, xtrax bridge, stdout redaction, capability probe,
signed-manifest fields, claim-calibration plumbing) and **praxia** (6 items: generic rig-run
dispatch, workspace-derived registry dirs, toolless-strict guard, AST-scoped SubFlow export
rewrite, gated strict-mode registry, export --check). These are edges out of this roadmap, filed
in their home repos; **nothing in the #2181 MVP critical path depends on the praxia items.**

## Dependency spine

```
xtrax 0.3.0 (released)
  │
  ├─ T1 Slice 1 — land topology validator (in-flight stages/topology.py)
  │      + D3 executed two-tier boundaries (ordered Tap/Sink in Vmap/SafeMap/Scan,
  │        JEP-10657 tokens; Fuse at run layer) + io_callback shim (import-time assert)
  │      │
  │      └─ T1 Slice 2 — D1 frozen-context host-prep graphs + D2 sealed EvaluateFn seam
  │             (core Protocol + registration lock) + D4 node-metadata round-trip
  │             + typed composition IR node (schema emitter, `graph validate` verb,
  │               generate-then-validate default, authoring benchmark) 
  │             │
  │             ├─ ENTRY EDGE → T2 #2181 walking skeleton
  │             │     (sealed evaluator monopoly, git-ratchet, schema-JSON info barrier,
  │             │      watchdog kill authority, crash-atomic ratchet)
  │             │     │
  │             │     ├─ T2 loop MVP (restart-from-best-K + diversity-quota Leap-Path;
  │             │     │   compile time out of fitness, separately gated)
  │             │     │     │
  │             │     │     └─ T2 confirmatory campaigns  ◄─── bathos build items
  │             │     │          (seed-gate hard block, stats battery, campaign_edges,
  │             │     │           capability probe AC-20 blocks start)
  │             │     │
  │             │     └─ Phase-2 (gated): island/GEAR upgrade — requires full GEAR+AutoSOTA read
  │             │
  │             └─ chain-map UI (deferred; consumes the same IR read-only)
  │
  └─ T3 phase 1 (independent, ships now): plugin packaging manifest + drift --check
        + first-consumer SubFlow test + TTL/probe human-gate freshness
        │
        └─ T3 phase 2 (praxia-repo milestone, gated on AC-V2 MCP-reachability probe):
              generic rig-run dispatch, strict-mode registry, port_validation strict variant
```

**Cross-session reconciliation:** T2's pre-mortem flagged fork 9 (RunSpec.boundaries keying) as
a hard blocker on its entry edge; T1's session resolved it (name-keying adapter at executor
entry, `RunSpec.boundaries` stays `list`, injectivity AC). No open conflict between sessions.

## Human (HITL) gates

1. **Roadmap approval** — this document + DAG docs, after adversarial review (you are here).
2. **#2181 constitution authorship** — the immutable top-level rules (research agenda, budgets,
   error handling) are human-authored before any loop runs.
3. **Campaign approval** — every confirmatory campaign pre-registration is human-approved.
4. **Promotion-to-main review** — no evolved candidate merges without human engineering review.
5. **Scope expansion** — adding evolve-block surface area or new effectful tools = human gate.
6. **Kill switch** — the watchdog's authority is unrevokable by the loop; humans hold it.
7. **T3 phase-2 entry** — investing in praxia-side strict dispatch is a human decision fed by
   the AC-V2 probe result.

## Metrics of success / fail-fast conventions (roadmap-level)

- **Every gate in every DAG states its success metric AND its fast/loud failure behavior** —
  inherited from the audit-framework template (deterministic spine, probabilistic leaves).
- T1: BatchPlan parity (zero divergence), ordering preserved under nested vmap-of-scan stress
  (fail on FIRST out-of-order record), authoring benchmark reports ABSOLUTE faithfulness (fails
  if only mode-vs-mode delta), compiler-boundary grep-gate (CI exit 1).
- T2: fitness scalars ONLY from the SHA-closure-pinned evaluator (AC-7 hashes the complete
  closure); synthetic ground-truth sanity check must pass before any campaign (BATHOS rule);
  statistical gates at campaign conclude: WR≥0.6, BP≥0.2, Cohen's d≥0.2, α=0.05 Holm, ≥3 seeds
  ICC>0.990 (hard block for confirmatory); no LLM judgment inside the loop.
- T3: install/export drift → CI exit 1 with diff; human-gate records derive from TTL
  attestation + invalidate-only probe (the n9-staleness defect is structurally killed);
  toolless-strict dispatch impossible by construction.
- **No silent caps anywhere:** every deferred/skipped item is a named decision in a spec's
  Decision Log, not an omission.

## Immediate next actions once approved

1. File the three DAGs into the praxia backlog (xtrax workspace) with `depends_on` edges;
   file bathos/praxia cross-repo items in their home workspaces.
2. Update `.praxia/loop_priorities.toml`: #2181 → spec'd/queued behind T1 Slice 2 entry edge;
   register T3 phase 1 as an executable maintenance thread; record locked decisions.
3. Land the in-flight topology validator (T1 Slice 1 opener) — it's test-green in the main
   checkout working tree.
4. Early research errand (cheap, unblocks Phase-2 planning): full read of GEAR
   (arXiv 2605.13874) + AutoSOTA before any island-search DAG node is finalized.

## Out of scope (named deferrals)

Chain-map UI (consumes IR later); island search day-one; K-Veritas full attestation
(signed-manifest interim chosen); type-guided decoding (TS-only evidence); Stitch
library-learning (no graph corpus yet); bathos_sidecar_ref code enforcement (slot kept);
per-step-carry Fuse (rejected loudly by validate_plan_topology).
