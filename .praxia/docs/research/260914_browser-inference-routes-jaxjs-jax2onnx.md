---
title: 'Browser inference routes for aminx: jax-js, jax2onnx, and the int32/quantization question'
description: Measured coverage of the non-IREE browser paths — jax-js is a reimplementation not a converter, jax2onnx takes Equinox directly and covers every primitive aminx needs, but ONNX mandates int64 TopK indices which ORT Web's WebGPU EP does not support
status: draft
task_id: 260914_browser-inference-routes
date: '260914'
confidence: 'medium — every coverage claim is read off a source file or an autogenerated table and cited to it; NOTHING has been executed against an aminx model, and jax2onnx is not installed here. Revised after an adversarial review (260914) that found six factual errors, including a false claim that aminx had already repinned and a §4 mitigation that contradicted its own premise; see §4 Consequence and §7, which record the corrections rather than hiding them.'
sources: 'github.com/ekzhang/jax-js; github.com/enpasos/jax2onnx (coverage tables + plugin source); onnx.ai operator spec; onnxruntime.ai web docs; iree-org/iree#24650; local measurement of iree-base-compiler 3.11.0'
---
# Browser inference routes for aminx: jax-js, jax2onnx, and the int32/quantization question

## Why this exists

The question asked was whether all aminx models are available as xtrax-exported
fixtures and whether inference could be served from a GitHub Pages interface.
Both answers are no (§1). The follow-up — "what about jax.js, or other tools for
more direct conversion to WebGPU and wasm kernels?" — turns out to have a real
answer that is *better* than the IREE route we have been pursuing, and this
document records the measurement so the next reader does not re-derive it.

**The one-line finding:** `jax2onnx` converts Equinox models directly and covers
every `jax.lax` primitive aminx uses, so the browser is reachable today without
waiting on IREE. The catch is not op coverage — it is that ONNX *mandates* int64
indices on the exact operator aminx's k-NN and tiebreak paths lower to, and ONNX
Runtime Web's WebGPU backend does not support int64.

## 1. Starting state (measured 260914)

- **aminx exports nothing.** No `xtrax.export` import, no `export_pipeline`
  call, no tracked `.vmfb`/`.mlir`/`.wasm` anywhere in the repo. xtrax's own
  export fixtures compile a locally-defined `TinyMLP` and never reference aminx.
- **The export toolchain is not installed in aminx yet.** aminx `origin/main`
  still pins `xtrax[io]==0.4.0a9` (`pyproject.toml:26`), and `[io]` does not
  pull the `export` extra — so neither the 0.4.0a10 refusal gates nor IREE
  itself are present in an aminx environment. The repin to
  `xtrax[io,export]==0.4.0a10` is **open, not merged** (aminx PR #158). Any
  spike below must merge or check out that branch first; do not budget on the
  assumption that the tooling is already there.
- **15 tracked checkpoints, 110 MB total** under `src/aminx/model_params/`
  (`git ls-files` returns 16 entries — one is `__init__.py`):
  ~5.9 MB each for `proteinmpnn`/`solublempnn` `v_48_*`, 9.3–13 MB for
  `ligandmpnn` `v_32_*`, 6.5 MB for the membrane variants.
- **IREE still has no WebGPU backend.** Re-measured today against the pinned
  `iree-base-compiler 3.11.0`:
  `cuda  llvm-cpu  metal-spirv  rocm  vmvx  vmvx-inline  vulkan-spirv`.
  No `webgpu`, no `webgpu-spirv`. Blocked upstream on
  [iree#24650](https://github.com/iree-org/iree/issues/24650), open since
  2026-06-29.

## 2. jax-js is a reimplementation, not a converter

[ekzhang/jax-js](https://github.com/ekzhang/jax-js) is a from-scratch
implementation of the JAX/NumPy API in JavaScript that JIT-compiles to WebGPU
and wasm kernels. Performance is real: >7000 GFLOP/s matmul on an M4 Max under
WebGPU, and the fastest multithreaded in-browser wasm matmul (~2× XNNPACK).

**It does not ingest Python JAX models.** No StableHLO, no `jax.export`, no
serialized-program path. There are exactly two ways in:

1. rewrite the model in JavaScript against its API, or
2. load an ONNX file through its `@jax-js/onnx` package.

So for our purposes jax-js is **not an export target** — it is one of several
*runtimes* that can consume an ONNX file. That reframes it entirely: the
interesting work is upstream of it. Maturity is early (~927 stars at time of
writing, no production-readiness claim); `@jax-js/loaders` reads safetensors and
caches large weight downloads in OPFS.

## 3. jax2onnx is the actual bridge — and it speaks Equinox

[enpasos/jax2onnx](https://github.com/enpasos/jax2onnx) converts JAX, Flax NNX,
Flax Linen, and **Equinox** callables to ONNX. aminx models are Equinox, so
there is no reimplementation step. It also ships an explicit `export_mode="web"`
and a browser/WASM validation workflow.

Primitive coverage, read off the autogenerated table
(`docs/user_guide/jax_lax_coverage.md`) rather than the README:

| Primitive | Status | Relevance to aminx |
|---|---|---|
| `cond` | direct plugin | `utils/coordinates.py:51` backbone-noise branch |
| `scan` | direct plugin | autoregressive decode |
| `sort` | direct plugin | **the only index-producing primitive in a traced aminx graph** — both `num_keys=2` tiebreaks (§5) |
| `top_k` | direct plugin | *not exercised* — see below |
| `gather`, `scatter`(+`_add/_max/_min/_mul/_sub`) | direct plugin | feature assembly |
| `while_loop` | covered indirectly (via `while`) | |
| `rng_bit_generator`, `rng_uniform` | direct plugin | |

**The table contains zero `not_covered` rows.** Unsupported primitives fail
loudly with an explicit error rather than silently miscompiling, and the plugin
system is the documented extension path.

**`top_k` coverage is irrelevant to an exported aminx graph, and that matters.**
aminx PR #156 removed `jax.lax.top_k` from every traced path: the k-NN, ligand
and packer sites all route through `aminx.model.features.top_k`, which is
implemented as `jax.lax.sort((-x, index), num_keys=2, is_stable=False)`
(`features.py:90`). The only surviving `jax.lax.top_k` calls are
`host/logit_aggregation.py:221` and `inference/schedule_selector.py:264`, both
eager host code never traced into an export. So `sort` is the **sole**
index-producing primitive in the graph — which makes §5 not one risk among
several but the entire index risk.

Note what this does to our live IREE blocker: `lax.cond` traces both branches,
which is why RNG is in the graph even at `backbone_noise=0` and why the IREE
path is stuck. `cond` converts cleanly here, so that specific blocker is not
inherently a blocker on this route.

## 4. The int32 question — aminx is already 32-bit; ONNX is not

This was the sharpest part of the question, and the answer has two halves that
point in opposite directions.

**Half one — aminx's library code is already int32.** `jax_enable_x64` is never
enabled anywhere under `src/`, so JAX operates 32-bit by default and every
traced array is `int32`/`float32`. The `int64`/`float64` occurrences in the tree
(`host/_sampling_grid_lineage.py`, `parity/evidence.py`,
`ebm/ddg_stability.py`) are host-side NumPy never traced into an export.

**But that guarantee is process-wide, not import-scoped — check it at runtime.**
`jax_enable_x64` is a global config flag, and two scripts set it
**unconditionally at module import and never restore it**:
`scripts/data_processing/process_parallel.py:14` and
`scripts/data_processing/debug_worker.py:18`. (The `tests/ebm/conftest.py`
fixture, by contrast, does set-and-restore.) An export run from a process that
has imported either script traces in x64 and silently produces an int64/float64
graph. So the correct claim is "aminx's *library* code does not enable x64", not
"aminx cannot produce int64" — and any export harness should assert
`jax.config.jax_enable_x64 is False` at entry rather than assume it.

**Half two — ONNX mandates int64 anyway, and that is the collision.** The ONNX
spec constrains `TopK`'s index output to `tensor(int64)` — it is not a choice
the exporter makes. And jax2onnx's `lax.sort` plugin
(`jax2onnx/plugins/jax/lax/sort.py`) lowers sort to **`TopK` + `GatherElements`**
and explicitly stamps the indices `INT64` at lines 190-191.

Meanwhile ONNX Runtime Web's **WebGPU execution provider does not support
int64**; the wasm EP supports all operators. So the int64 index tensors land
precisely on the WebGPU backend's known dtype gap — and they land on the index
path, which is the exact path that has already produced two silent bugs in this
model (tie order, random permutation).

**Consequence — and an earlier draft of this document got it wrong, so the
correction is recorded rather than quietly patched.** The tempting fix is
"narrow the indices to int32 offline after export." **That cannot work for
`TopK` itself.** If the spec mandates `tensor(int64)` for its index output, then
rewriting that output's dtype produces a spec-invalid graph, and inserting a
`Cast` *after* `TopK` leaves `TopK`'s own int64 output tensor sitting in the
graph — still requiring the WebGPU EP to materialize precisely the dtype it does
not support. Offline narrowing helps only for int64 that *we* introduced;
here we introduced none (half one), so it buys nothing.

**The mechanism that actually applies is ONNX Runtime's per-node EP fallback.**
Nodes the WebGPU EP cannot take are assigned to the wasm/CPU EP, and the graph
is partitioned with device copies at each boundary. The expected outcome is
therefore not a load-time rejection but a **partitioned graph with copy
overhead** — and, more importantly, a model whose index-producing nodes execute
on a *different* backend from its matmuls. That is worth stating precisely
because it means the WebGPU run and the wasm run may not agree on tie order
even for the same file. **Unverified for this model; it is the first thing
step 3 of §9 should measure.**

Practical ordering that falls out of this: **wasm EP first, WebGPU second.**
wasm supports every operator and every dtype, so it is the correctness
reference; WebGPU is the optimization, and it is the one that can silently
change integer results.

## 5. HYPOTHESIS (unverified): multi-key sort re-introduces a stability dependency

aminx replaced inherited sort stability with an explicit index tiebreak —
`jax.lax.sort((-x, index), num_keys=2, is_stable=False)` — specifically so that
no backend's tie handling could permute the result. That was the right fix and
it remains correct in JAX.

**There are TWO such sites, not one**, and both are on traced paths:

| Site | Sort keys | Path | Landed |
|---|---|---|---|
| `model/features.py:90` | `(-x, index)` | k-NN neighbour selection | PR #156 |
| `utils/decoding_order.py:104` | `(step, index)` | autoregressive decode order | PR #157 |

The decode-order site is the more consequential of the two: it determines the
sequence in which residues are generated, so a permutation there changes the
whole design, not one neighbour list.

But jax2onnx's sort plugin implements multi-key sort as a **radix pass over keys
in reverse order** (`for key_idx in reversed(range(num_keys))`, sort.py:115),
emitting one `TopK` per key. An LSD radix sort is only correct if **each pass is
stable**: the index pass runs first, then the value pass must preserve that
ordering for equal values.

If ONNX `TopK` does not guarantee stable tie order — and ONNX Runtime's WebGPU
and wasm EPs need not agree with each other about it, which §4's EP-partitioning
note makes concretely likely — then the tiebreak aminx added is re-exposed to
exactly the failure class it was written to eliminate, one layer down in the
ONNX graph. Because `sort` is the *only* index-producing primitive in the traced
graph (§3), this is not one risk among several; it is the index risk.

(Strictly, an LSD radix sort's *first* pass need not be stable — only the later
ones. That does not weaken the conclusion here, and the first pass is over
distinct indices anyway.)

**This is a hypothesis, not a finding.** It has not been tested. It is written
down because it is cheap to test, it is invisible to float parity, and it is the
single most likely way this route produces a green check over wrong output. See
[[reference_iree-breaks-jax-stable-sort]] for the identical failure on the IREE
route.

## 6. Quantization — the acceptance metric has to change

Weight size is **not** the constraint here. A single `proteinmpnn` checkpoint is
~5.9 MB compressed (~6.8 MB raw fp32) — trivially downloadable. fp16 halves it,
int8 quarters it. The 110 MB figure only matters if you intend to ship all 16
checkpoints to the browser, and GitHub Pages' limits (100 MB/file, ~1 GB repo)
would accommodate even that. **Quantize for latency and memory if profiling says
to, not for download size.**

The real issue is what quantization does to *this* model's output. aminx emits a
**discrete sequence**, selected by argmax/top-k over logits. Every export defect
found so far has been an integer divergence with bit-identical floats. Coarsening
the logits:

- makes exact ties **more** frequent, not less, which increases dependence on the
  tie-order behaviour §5 flags as unverified;
- can flip an argmax between two near-equal amino acids while leaving logit MSE
  and `allclose` looking excellent.

So the acceptance criterion for any quantized aminx artifact must be
**sequence identity / recovery rate against the JAX oracle**, not logit MSE, not
`allclose`, and not float `max_abs_diff`. jax2onnx's own docs recommend
`allclose` for parity — that is necessary and **not sufficient** for this model.
Suggested ladder, cheapest-first: fp32 → fp16 → int8 weight-only/dynamic on
linear ops, stopping at the first tier where sequence recovery degrades.

## 7. What this route would change architecturally

Under IREE, weights **bake into the module as constants**, so the artifact
matrix is `checkpoints × buckets` — which is what forced the
`(64,128,256,512,1024,1536,2048)` ladder and its ~28 MB bundled tier.

Under ONNX, weights are **initializers in a single file** and jax2onnx supports
symbolic dimensions. If sequence length can be made a symbolic dim, the bucket
ladder may collapse to **one artifact per checkpoint**. That would be a large
simplification.

**But this is blocked on an aminx source change, not on an experiment** — a
correction to an earlier draft that called it merely doubtful. The clamp is
`k = min(self.k_neighbors, structure_coordinates.shape[0])`
(`model/features.py:227`), and its result is used as a **static slice width**,
`order[..., :k]` (`features.py:91`). That is a Python `min()` over a concrete
`.shape[0]`, not a traced operation. Under a symbolic leading dimension
`shape[0]` becomes a symbolic expression, `min()` on it raises at trace time,
and even if it did not, a symbolic `k` cannot index a static slice. So dynamic
`L` cannot be attempted until that expression is rewritten in traced form
(e.g. a fixed `k` with an `+inf`-masked distance matrix, which is already how
padded residues are excluded).

Until then the bucket ladder stands. The padding-invariance result
(score-preserving iff `L ≥ k_neighbors`) means bucketing remains correct, so
this costs artifact count, not correctness.

## 8. Route comparison

| Route | Browser today? | Blocker |
|---|---|---|
| IREE `wasm32` | no | CODEGEN_ONLY; needs an emsdk-built IREE runtime, no published package |
| IREE `webgpu` | no | backend absent from every published wheel; iree#24650 |
| IREE `native-portable` | no | x86-64-v2 ELF, needs local IREE runtime |
| **jax2onnx → ORT Web (wasm)** | **yes** | int64 fine here; slowest tier |
| **jax2onnx → ORT Web (WebGPU)** | **yes, with caveats** | int64 unsupported → index nodes expected to fall back to CPU EP with copy cost (§4), *not* narrowable offline; op subset |
| jax2onnx → @jax-js/onnx | plausible | jax-js maturity unproven for this model |
| jax2onnx → wonnx | plausible | Rust/WebGPU, less exercised |
| rewrite in jax-js | yes | full model reimplementation, ongoing divergence risk |

## 9. Recommended next step

A bounded spike, roughly a day. **Step 0 is not optional:** aminx's environment
does not yet have the export toolchain (§1) — merge or check out aminx PR #158
first, and assert `jax.config.jax_enable_x64 is False` in the harness (§4).

1. `jax2onnx` one checkpoint (`v_48_020`) at a fixed bucket length ≥ 48.
   Dynamic `L` is out of scope — §7 shows it needs an aminx source change first.
2. Run under ORT Web **wasm** EP (the correctness reference), then **WebGPU**.
   Record the EP partitioning: which nodes the WebGPU EP refused, and whether
   the int64 index nodes fell back to CPU as §4 predicts.
3. Diff against the JAX oracle on **both** logits *and* emitted indices /
   decoded sequence — the index diff is the whole point; a float-only check
   passes vacuously here.
4. Check the §5 tiebreak at **both** sites — `features.py:90` (neighbour order)
   and `decoding_order.py:104` (decode order). Verifying only the first would
   report a false all-clear for the path that actually determines the design.
   Also diff wasm against WebGPU directly: if they disagree on ties, the
   partitioning in step 2 is the likely cause.
5. Only then the quantization ladder (§6), scored on sequence recovery.

This reuses the parity discipline already built for the IREE path and answers the
viability question without committing to the route.

## 10. What is NOT claimed here

- jax2onnx has **not** been run against an aminx model. Coverage-table support is
  not the same as "it converts."
- **jax2onnx is not installed on this machine.** The three claims cited to its
  source — the zero-`not_covered` coverage table, the `TopK`+`GatherElements`
  lowering, and the `INT64` stamp at `sort.py:190-191` — were read from the
  repository at `main` over the network, not executed. They are load-bearing for
  §3 and §5, and a version skew between that `main` and whatever version a spike
  installs would invalidate them.
- §5 (sort stability) is a hypothesis with a stated reason, not a measurement.
  §7 is the opposite: a stated blocker, not an open experiment.
- §4's EP-partitioning prediction is the expected ORT behaviour, not something
  observed for this model.
- No statement is made about Firefox or Safari WebGPU status.
- This does not supersede
  `.praxia/docs/specs/260910_webgpu-export-route.md`; that spec governs the IREE
  route's verification levels and its constraint — *no artifact may imply browser
  compatibility without browser-execution evidence* — applies verbatim to
  everything above.
